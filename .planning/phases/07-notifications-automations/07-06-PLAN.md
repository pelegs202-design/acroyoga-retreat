---
phase: 07-notifications-automations
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/cron/jam-reminders/route.ts
autonomous: true
requirements: [NOTIF-03]
gap_closure: true

must_haves:
  truths:
    - "Challenge participant with a phone number on file receives a WhatsApp session reminder the day before and morning of each jam session"
    - "WhatsApp send uses the phone number from challengeEnrollments or quizLeads (looked up by attendee email)"
    - "If no phone number is found for an attendee, WhatsApp is skipped gracefully and email still sends"
  artifacts:
    - path: "src/app/api/cron/jam-reminders/route.ts"
      provides: "Phone number lookup via challengeEnrollments/quizLeads join by email, then WhatsApp send"
      contains: "challengeEnrollments"
  key_links:
    - from: "src/app/api/cron/jam-reminders/route.ts"
      to: "src/lib/whatsapp.ts"
      via: "sendWhatsAppTemplate with phone from enrollment/lead lookup"
      pattern: "sendWhatsAppTemplate.*userPhone"
---

<objective>
Fix the dead WhatsApp code path in the jam-reminders cron so that challenge participants actually receive WhatsApp session reminders.

Purpose: NOTIF-03 requires WhatsApp reminders day-before and morning-of each challenge session. Currently the cron tries to read `user.phone` which does not exist on the user table, so WhatsApp is silently skipped for all attendees. The phone number is available on `challengeEnrollments.customerPhone` and `quizLeads.phone` (linked by email). This plan fixes the lookup without requiring a schema migration.

Output: Updated jam-reminders cron that resolves attendee phone numbers from existing tables.
</objective>

<execution_context>
@C:/Users/somet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/somet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/acroyoga-academy/.planning/PROJECT.md
@C:/acroyoga-academy/.planning/ROADMAP.md
@C:/acroyoga-academy/.planning/STATE.md
@C:/acroyoga-academy/.planning/phases/07-notifications-automations/07-03-SUMMARY.md

@C:/acroyoga-academy/src/app/api/cron/jam-reminders/route.ts
@C:/acroyoga-academy/src/lib/db/schema.ts
@C:/acroyoga-academy/src/lib/whatsapp.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix phone number lookup in jam-reminders cron</name>
  <files>src/app/api/cron/jam-reminders/route.ts</files>
  <action>
Replace the dead `(attendeeUser as { phone?: string }).phone` cast (line 169) with a real phone number lookup.

After fetching the attendee user details (around line 115), add a phone lookup query that:
1. Import `challengeEnrollments` and `quizLeads` from `@/lib/db/schema`
2. Query `challengeEnrollments` for a row where `customerEmail` equals `attendeeUser.email` and `status = 'confirmed'`, selecting `customerPhone`. Use `.limit(1)` and order by `paidAt desc` (most recent enrollment).
3. If no phone found from enrollments, fall back to querying `quizLeads` where `email` equals `attendeeUser.email`, selecting `phone`. Use `.limit(1)` and order by `createdAt desc`.
4. Assign the result to `userPhone` (replacing the broken cast on line 169).

The rest of the WhatsApp send logic (lines 170-182) stays exactly the same — the `if (userPhone)` guard and `sendWhatsAppTemplate` call are correct; they just need a real phone value.

Remove the misleading comments on lines 154-157 that say "Phase 7+ enhancement" and "send WhatsApp only if we have phone from prefs metadata". Replace with a clear comment: "Look up phone from challengeEnrollments (paid) or quizLeads (quiz taker) by email".

Do NOT modify the email sending logic (lines 185-214). Do NOT modify the schema. Do NOT add a phone column to the user table.
  </action>
  <verify>
1. `npx tsc --noEmit` passes (no type errors in the modified file)
2. Grep the file for `challengeEnrollments` — import and query both present
3. Grep the file for `(attendeeUser as` — the unsafe cast is gone
4. The `sendWhatsAppTemplate` call is still present and reachable when `userPhone` is truthy
5. The email send block (lines 185+) is unchanged
  </verify>
  <done>
The jam-reminders cron resolves attendee phone numbers from challengeEnrollments (primary) or quizLeads (fallback) by email match. WhatsApp session reminders will now actually send for any attendee who has a phone number on record from their quiz submission or challenge payment. Attendees with no phone on either table gracefully fall through to email-only (existing behavior).
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no type errors project-wide
- File contains `import { ... challengeEnrollments, quizLeads ... } from "@/lib/db/schema"`
- File does NOT contain `(attendeeUser as { phone?: string })` unsafe cast
- File does NOT contain "Phase 7+ enhancement" comment
- `sendWhatsAppTemplate` call is still present inside the `if (userPhone)` block
- Email send block is untouched
</verification>

<success_criteria>
NOTIF-03 gap is closed: the jam-reminders cron can now resolve a phone number for challenge participants and send WhatsApp session reminders. The fix uses existing data (challengeEnrollments.customerPhone / quizLeads.phone) without requiring a schema migration.
</success_criteria>

<output>
After completion, create `.planning/phases/07-notifications-automations/07-06-SUMMARY.md`
</output>
