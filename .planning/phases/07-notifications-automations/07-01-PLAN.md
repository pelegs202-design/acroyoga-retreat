---
phase: 07-notifications-automations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/push.ts
  - src/lib/whatsapp.ts
  - src/lib/notifications/index.ts
  - src/lib/notifications/drip-sequences.ts
  - src/app/sw.ts
  - vercel.json
  - .env.example
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04, NOTIF-05]

must_haves:
  truths:
    - "Database can store push subscriptions, notification preferences, push queue entries, and drip enrollments"
    - "Service worker handles push events and notification click deep-links"
    - "Server can send VAPID push notifications via web-push library"
    - "Server can send WhatsApp template messages via Meta Cloud API"
    - "Vercel cron jobs are configured for push batching, WhatsApp drip, email drip, and jam reminders"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "pushSubscriptions, notificationPreferences, pushQueue, dripEnrollments tables"
      contains: "pushSubscriptions"
    - path: "src/lib/push.ts"
      provides: "VAPID setup and sendPushToUser function"
      exports: ["sendPushToUser"]
    - path: "src/lib/whatsapp.ts"
      provides: "sendWhatsAppTemplate function wrapping Meta Cloud API"
      exports: ["sendWhatsAppTemplate", "normalizeIsraeliPhone"]
    - path: "src/lib/notifications/drip-sequences.ts"
      provides: "Sequence definitions with step count, spacing, and template names"
      exports: ["DRIP_SEQUENCES"]
    - path: "src/app/sw.ts"
      provides: "Push event listener and notificationclick deep-link handler"
      contains: "addEventListener.*push"
    - path: "vercel.json"
      provides: "Cron job schedule definitions"
      contains: "crons"
  key_links:
    - from: "src/app/sw.ts"
      to: "push notification display"
      via: "self.addEventListener('push')"
      pattern: "self\\.addEventListener.*push"
    - from: "src/lib/push.ts"
      to: "web-push library"
      via: "webpush.setVapidDetails + sendNotification"
      pattern: "webpush\\.sendNotification"
    - from: "src/lib/whatsapp.ts"
      to: "Meta Graph API"
      via: "fetch to graph.facebook.com"
      pattern: "graph\\.facebook\\.com"
---

<objective>
Notification foundation — database tables, core libraries, service worker push handlers, and Vercel cron configuration.

Purpose: Every notification feature (push, WhatsApp, email drip) depends on shared schema tables and utility libraries. This plan creates the foundation that all Wave 2 plans build on.
Output: 4 new DB tables, push/whatsapp/email utility modules, extended service worker, vercel.json cron config.
</objective>

<execution_context>
@C:/Users/somet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/somet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/acroyoga-academy/.planning/PROJECT.md
@C:/acroyoga-academy/.planning/ROADMAP.md
@C:/acroyoga-academy/.planning/STATE.md
@C:/acroyoga-academy/.planning/phases/07-notifications-automations/07-CONTEXT.md
@C:/acroyoga-academy/.planning/phases/07-notifications-automations/07-RESEARCH.md
@C:/acroyoga-academy/src/lib/db/schema.ts
@C:/acroyoga-academy/src/app/sw.ts
@C:/acroyoga-academy/src/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database schema + notification libraries</name>
  <files>
    src/lib/db/schema.ts
    src/lib/push.ts
    src/lib/whatsapp.ts
    src/lib/notifications/index.ts
    src/lib/notifications/drip-sequences.ts
    .env.example
  </files>
  <action>
**Install dependencies:**
```bash
npm install web-push @types/web-push react-email @react-email/components
```

**Generate VAPID keys** (one-time, add to .env.example):
```bash
npx web-push generate-vapid-keys
```

**Extend src/lib/db/schema.ts** — add after the challengeEnrollments table (before Relations section):

1. `pushSubscriptions` table: id (text PK), userId (text, FK to user.id cascade), endpoint (text, unique), p256dh (text), auth (text), userAgent (text nullable), createdAt (timestamp). Index on userId.

2. `notificationPreferences` table: userId (text PK, FK to user.id cascade), pushEnabled (boolean default true), emailMarketing (boolean default true), whatsappEnabled (boolean default true), quietHoursStart (integer default 22), quietHoursEnd (integer default 8), updatedAt (timestamp with $onUpdate). Per user decision: defaults all ON, quiet hours 22:00-08:00.

3. `pushQueue` table: id (text PK), userId (text, FK to user.id cascade), eventType (text — 'new_message' | 'jam_rsvp' | 'review' | 'partner_match'), title (text), body (text), deepLink (text), batchKey (text nullable), queuedAt (timestamp defaultNow), sentAt (timestamp with timezone nullable), heldUntil (timestamp with timezone nullable — for quiet hours). Index on (userId, sentAt).

4. `dripEnrollments` table: id (text PK), leadId (text), userId (text nullable), sequenceType (text — 'wa_challenge_prepay' | 'wa_challenge_postpay' | 'email_nurture' | 'wa_workshop' | 'email_challenge_reminders'), channel (text — 'whatsapp' | 'email'), recipientPhone (text nullable), recipientEmail (text nullable), recipientName (text), preferredLocale (text default 'he'), currentStep (integer default 0), totalSteps (integer), nextFireAt (timestamp with timezone nullable), completedAt (timestamp with timezone nullable), cancelledAt (timestamp with timezone nullable), cancelReason (text nullable — 'paid' | 'opted_out' | 'expired'), metadata (text nullable — JSON string for extra data like cohortStartDate, archetype), createdAt (timestamp defaultNow). Indexes on nextFireAt, leadId, sequenceType.

Add relations for pushSubscriptions (one user), notificationPreferences (one user), pushQueue (one user), dripEnrollments (no FK — leadId may not be a user).

**Create src/lib/push.ts:**
- Import web-push, call `webpush.setVapidDetails('mailto:shai@acroretreat.co.il', process.env.NEXT_PUBLIC_VAPID_PUBLIC_KEY!, process.env.VAPID_PRIVATE_KEY!)`
- Export `sendPushToUser(subscription: { endpoint, p256dh, auth }, payload: { title, body, url })` — calls webpush.sendNotification with JSON.stringify payload, TTL 24h, urgency 'normal'. Catches 410 errors and rethrows with code 'SUBSCRIPTION_EXPIRED' so callers know to delete the subscription.

**Create src/lib/whatsapp.ts:**
- Export `sendWhatsAppTemplate({ to, templateName, languageCode, bodyParams, buttons })` — direct fetch to `https://graph.facebook.com/v21.0/${WA_PHONE_NUMBER_ID}/messages` with Bearer token from WA_CLOUD_API_TOKEN env var. Per research Pattern 5. Include error handling that throws with the API error body.
- Export `normalizeIsraeliPhone(raw: string): string` — strips non-digits, converts 05x prefix to 9725x (drops leading 0, prepends 972). If already starts with 972, return as-is.

**Create src/lib/notifications/index.ts:**
- Export `queuePushNotification(userId, eventType, title, body, deepLink, batchKey?)` — inserts into pushQueue table. Checks quiet hours: if current Israel time (UTC+3) is between user's quietHoursStart and quietHoursEnd, sets heldUntil to next quietHoursEnd. Import db and schema.
- Export `enrollInDrip({ leadId, userId?, sequenceType, channel, recipientPhone?, recipientEmail?, recipientName, preferredLocale, metadata? })` — looks up sequence from DRIP_SEQUENCES to get totalSteps, calculates initial nextFireAt (tomorrow at 19:00 Israel time for WA, or immediate for email confirmations), inserts into dripEnrollments.
- Export `cancelDrip(leadId, sequenceType, reason)` — sets cancelledAt=now(), cancelReason on matching active enrollment.

**Create src/lib/notifications/drip-sequences.ts:**
- Export `DRIP_SEQUENCES` object defining all sequence types:
  - `wa_challenge_prepay`: 7 steps, channel 'whatsapp', spacing starts at 2 days then tightens to daily (array of day offsets: [0, 2, 4, 6, 7, 8, 9]), template names per step (e.g., 'challenge_welcome_he', 'challenge_tip1_he', etc.)
  - `wa_challenge_postpay`: 5 steps, channel 'whatsapp', spacing [0, 1, 2, 3, 4] days from enrollment
  - `wa_workshop`: 3 steps, channel 'whatsapp', spacing [0, 2, 5] days
  - `email_nurture`: 12 steps, channel 'email', spacing: first 5 at 7-day intervals, then remaining at 14-day intervals. After step 12, cycle restarts from step 6 (rotating templates — set nextFireAt to step 6 position instead of marking complete).
  - `email_challenge_reminders`: dynamic steps (computed from cohort schedule), channel 'email'
- Each sequence entry has: steps (number), channel, spacingDays (number[]), templates (array of { templateName, subject? } per step).

**Update .env.example** with new env vars:
```
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
WA_PHONE_NUMBER_ID=
WA_CLOUD_API_TOKEN=
CRON_SECRET=
```
  </action>
  <verify>
    - `npx tsc --noEmit` passes (schema types valid, no import errors)
    - New tables defined: `pushSubscriptions`, `notificationPreferences`, `pushQueue`, `dripEnrollments`
    - `src/lib/push.ts` exports `sendPushToUser`
    - `src/lib/whatsapp.ts` exports `sendWhatsAppTemplate` and `normalizeIsraeliPhone`
    - `src/lib/notifications/index.ts` exports `queuePushNotification`, `enrollInDrip`, `cancelDrip`
  </verify>
  <done>Schema has 4 new tables with correct types and indexes. Push, WhatsApp, and notification orchestration libraries export working functions. VAPID keys in .env.example.</done>
</task>

<task type="auto">
  <name>Task 2: Service worker push handlers + Vercel cron config</name>
  <files>
    src/app/sw.ts
    vercel.json
  </files>
  <action>
**Extend src/app/sw.ts** — add push and notificationclick event listeners BEFORE the `serwist.addEventListeners()` call (critical — Serwist takes ownership of the event loop after that call):

```typescript
// MUST come BEFORE serwist.addEventListeners()
self.addEventListener('push', (event: PushEvent) => {
  if (!event.data) return;
  const data = event.data.json() as { title: string; body: string; url: string };
  event.waitUntil(
    self.registration.showNotification(data.title, {
      body: data.body,
      icon: '/icons/icon-192x192.png',
      badge: '/icons/badge-72x72.png',
      data: { url: data.url },
      dir: 'auto',  // browser auto-detects RTL for Hebrew
    })
  );
});

self.addEventListener('notificationclick', (event: NotificationEvent) => {
  event.notification.close();
  const targetUrl = event.notification.data?.url ?? '/';
  event.waitUntil(
    self.clients.matchAll({ type: 'window', includeUncontrolled: true }).then((clients) => {
      const existing = clients.find((c) => c.url.includes(targetUrl));
      if (existing) return existing.focus();
      return self.clients.openWindow(targetUrl);
    })
  );
});
```

Update the `declare const self` type to include ServiceWorkerGlobalScope types needed for push/notification events. Keep the existing `any` cast approach with `__SW_MANIFEST` but ensure PushEvent and NotificationEvent types work.

**Create vercel.json** at project root:
```json
{
  "crons": [
    { "path": "/api/cron/push-batch", "schedule": "*/5 * * * *" },
    { "path": "/api/cron/whatsapp-drip", "schedule": "0 16 * * *" },
    { "path": "/api/cron/email-drip", "schedule": "0 16 * * *" },
    { "path": "/api/cron/jam-reminders", "schedule": "0 5,15 * * *" }
  ]
}
```

Note on cron times (all UTC, Israel is UTC+3 in summer):
- push-batch: every 5 min (requires Vercel Pro plan)
- whatsapp-drip: 16:00 UTC = 19:00 Israel (evening send time per user decision)
- email-drip: 16:00 UTC = 19:00 Israel
- jam-reminders: 05:00 UTC = 08:00 Israel (morning) + 15:00 UTC = 18:00 Israel (evening)

Run `drizzle-kit push` to apply schema changes to Neon.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `src/app/sw.ts` contains `addEventListener('push'` BEFORE `serwist.addEventListeners()`
    - `vercel.json` exists with 4 cron definitions
    - `drizzle-kit push` succeeds (tables created in Neon)
  </verify>
  <done>Service worker handles push display with deep-link click-through. Vercel cron jobs defined for all 4 notification dispatchers. Schema pushed to database.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Schema file has 4 new table definitions with proper indexes and foreign keys
- Service worker has push + notificationclick listeners before serwist.addEventListeners()
- vercel.json exists with correct cron schedules
- All 3 core libs (push.ts, whatsapp.ts, notifications/index.ts) export their functions
- drip-sequences.ts defines all 5 sequence types with step counts and spacing
</verification>

<success_criteria>
Foundation layer complete: database schema for all notification state, utility libraries for push/WhatsApp/email orchestration, service worker push handling, and Vercel cron scheduling — ready for Wave 2 plans to build channel-specific features on top.
</success_criteria>

<output>
After completion, create `.planning/phases/07-notifications-automations/07-01-SUMMARY.md`
</output>
