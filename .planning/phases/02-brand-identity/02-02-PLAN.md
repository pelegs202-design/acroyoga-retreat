---
phase: 02-brand-identity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/[locale]/layout.tsx
  - src/app/manifest.ts
  - src/app/~offline/page.tsx
  - src/components/pwa/InstallPrompt.tsx
  - src/lib/auth.ts
  - messages/en.json
  - messages/he.json
autonomous: true
requirements:
  - BRAND-01

must_haves:
  truths:
    - "Every user-visible surface displays 'AcroHavura' (English) or 'אקרוחבורה' (Hebrew) — never 'AcroYoga Academy' or 'AcroAcademy'"
    - "The offline page and install prompt display the new brand name AcroHavura"
    - "The password reset email comes from 'AcroHavura' with direct, bold voice — not 'AcroYoga Academy'"
    - "PWA manifest name is 'AcroHavura' and short_name is 'AcroHavura' (11 chars, fits iOS limit)"
    - "Browser tab title shows 'AcroHavura' on all pages"
  artifacts:
    - path: "src/app/manifest.ts"
      provides: "PWA manifest with AcroHavura name and short_name"
      contains: "AcroHavura"
    - path: "messages/en.json"
      provides: "English i18n strings with AcroHavura brand name"
      contains: "AcroHavura"
    - path: "messages/he.json"
      provides: "Hebrew i18n strings with אקרוחבורה brand name"
      contains: "אקרוחבורה"
    - path: "src/lib/auth.ts"
      provides: "Email sender name and subject using AcroHavura"
      contains: "AcroHavura"
    - path: "src/app/~offline/page.tsx"
      provides: "Offline page with AcroHavura brand name"
      contains: "AcroHavura"
    - path: "src/components/pwa/InstallPrompt.tsx"
      provides: "Install prompt with AcroHavura brand name"
      contains: "AcroHavura"
  key_links:
    - from: "src/app/[locale]/layout.tsx"
      to: "Browser tab title"
      via: "export const metadata title field"
      pattern: "AcroHavura"
    - from: "messages/en.json"
      to: "src/components/layout/Header.tsx"
      via: "useTranslations('common').appName"
      pattern: "appName.*AcroHavura"
    - from: "messages/he.json"
      to: "src/components/layout/Header.tsx"
      via: "useTranslations('common').appName in Hebrew locale"
      pattern: "appName.*אקרוחבורה"
---

<objective>
Rename all brand references from "AcroYoga Academy" to "AcroHavura" (אקרוחבורה) across 8 files.

Purpose: Replace every occurrence of the old placeholder brand name so no user-visible surface still says "AcroYoga Academy" or "AcroAcademy". This covers metadata, i18n strings, PWA manifest, offline page, install prompt, and email sender. After this plan, the brand is AcroHavura everywhere text appears.

Output: All 8 text/code files updated with AcroHavura / אקרוחבורה. Zero grep hits for old brand names.
</objective>

<execution_context>
@C:/Users/somet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/somet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-brand-identity/02-RESEARCH.md

Key source files (read before modifying):
@src/app/layout.tsx
@src/app/[locale]/layout.tsx
@src/app/manifest.ts
@src/app/~offline/page.tsx
@src/components/pwa/InstallPrompt.tsx
@src/lib/auth.ts
@messages/en.json
@messages/he.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename brand in metadata, manifest, and i18n strings</name>
  <files>
    src/app/layout.tsx
    src/app/[locale]/layout.tsx
    src/app/manifest.ts
    messages/en.json
    messages/he.json
  </files>
  <action>
Replace every occurrence of "AcroYoga Academy", "AcroAcademy", "אקדמיית אקרויוגה" with the new brand name across 5 files. Brand name details:

- **English name:** AcroHavura
- **Hebrew name:** אקרוחבורה
- **PWA short_name:** AcroHavura (11 chars — fits iOS 12-char limit)

**File-by-file changes:**

1. **`src/app/layout.tsx`** — Update `metadata.title` from `"AcroYoga Academy"` to `"AcroHavura"`.

2. **`src/app/[locale]/layout.tsx`** — Update `metadata.title` from `"AcroYoga Academy"` to `{ default: "AcroHavura", template: "%s | AcroHavura" }`. This enables per-page title suffixing in future phases.

3. **`src/app/manifest.ts`** — Update `name` from `"AcroYoga Academy"` to `"AcroHavura"`. Update `short_name` from `"AcroAcademy"` to `"AcroHavura"`. Update `description` to `"Find your perfect acro partner"` (matches homepage tagline).

4. **`messages/en.json`** — Replace all 4 occurrences of "AcroYoga Academy":
   - `common.appName`: `"AcroYoga Academy"` -> `"AcroHavura"`
   - `home.title`: `"Welcome to AcroYoga Academy"` -> `"Welcome to AcroHavura"`
   - `tosContent.intro.heading`: `"Welcome to AcroYoga Academy"` -> `"Welcome to AcroHavura"`
   - `tosContent.intro.body`: `"AcroYoga Academy (\"the Platform\")"` -> `"AcroHavura (\"the Platform\")"`

5. **`messages/he.json`** — Replace all 4 occurrences of "אקדמיית אקרויוגה":
   - `common.appName`: -> `"אקרוחבורה"`
   - `home.title`: -> `"ברוכים הבאים לאקרוחבורה"`
   - `tosContent.intro.heading`: -> `"ברוכים הבאים לאקרוחבורה"`
   - `tosContent.intro.body`: -> `"אקרוחבורה (\"הפלטפורמה\")"`
  </action>
  <verify>
Run: `grep -r "AcroYoga\|AcroAcademy\|אקדמיית אקרויוגה" src/app/layout.tsx src/app/\[locale\]/layout.tsx src/app/manifest.ts messages/en.json messages/he.json`
Must return zero lines.

Run: `grep -c "AcroHavura" messages/en.json` — must return at least 4.
Run: `grep -c "אקרוחבורה" messages/he.json` — must return at least 4.
  </verify>
  <done>All 5 files updated. Browser tab titles, PWA manifest, and i18n strings all show AcroHavura / אקרוחבורה. No old brand references remain in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Rename brand in offline page, install prompt, and auth email</name>
  <files>
    src/app/~offline/page.tsx
    src/components/pwa/InstallPrompt.tsx
    src/lib/auth.ts
  </files>
  <action>
Replace brand name in the 3 remaining files that use hardcoded strings (not i18n).

**File-by-file changes:**

1. **`src/app/~offline/page.tsx`** — Two changes:
   - `<title>AcroYoga Academy — Offline</title>` -> `<title>AcroHavura — Offline</title>`
   - The branded header div text `AcroYoga Academy` -> `AcroHavura`

2. **`src/components/pwa/InstallPrompt.tsx`** — One change:
   - `"Install AcroYoga Academy"` -> `"Install AcroHavura"`

3. **`src/lib/auth.ts`** — Two changes:
   - `FROM_EMAIL` default: `"AcroYoga Academy <noreply@acro.academy>"` -> `"AcroHavura <noreply@acro.academy>"` (keep the email domain — it's the Resend-verified sender domain, not the brand domain)
   - Email subject: `"Reset your AcroYoga Academy password"` -> `"Reset your password."` (direct, bold brand voice per CONTEXT.md — no brand name in subject, just action. Period is intentional — punchy, not friendly.)

**After all replacements, verify no old brand name remains anywhere in src/ or messages/.**
  </action>
  <verify>
Run: `grep -r "AcroYoga\|AcroAcademy\|אקדמיית אקרויוגה" src/ messages/ --include="*.tsx" --include="*.ts" --include="*.json" | grep -v node_modules`
Must return zero lines.

Run: `grep -r "AcroHavura" src/ messages/ --include="*.tsx" --include="*.ts" --include="*.json" | wc -l`
Must return at least 8 matches (across all files).
  </verify>
  <done>All 8 files updated. Zero remaining references to "AcroYoga Academy", "AcroAcademy", or "אקדמיית אקרויוגה" in src/ or messages/. The new brand "AcroHavura" / "אקרוחבורה" appears on every surface: browser tab, PWA name, offline page, install prompt, email sender, i18n strings. Email subject uses direct bold voice.</done>
</task>

</tasks>

<verification>
1. `grep -r "AcroYoga\|AcroAcademy\|אקדמיית אקרויוגה" src/ messages/ --include="*.tsx" --include="*.ts" --include="*.json"` returns zero matches
2. `grep -c "AcroHavura" messages/en.json` returns at least 4
3. `grep -c "אקרוחבורה" messages/he.json` returns at least 4
4. `grep "AcroHavura" src/app/manifest.ts` shows both name and short_name
5. `grep "AcroHavura" src/app/~offline/page.tsx` shows updated offline page
6. `grep "AcroHavura" src/components/pwa/InstallPrompt.tsx` shows updated install prompt
7. `grep "AcroHavura" src/lib/auth.ts` shows updated email sender
</verification>

<success_criteria>
- Brand name "AcroHavura" (אקרוחבורה) replaces all old name references across all 8 files
- PWA manifest shows AcroHavura name and short_name (11 chars)
- Browser tab titles show AcroHavura
- Password reset email uses AcroHavura sender name with direct bold voice
- Offline page and install prompt both show AcroHavura
- Zero grep hits for old brand names in src/ and messages/
</success_criteria>

<output>
After completion, create `.planning/phases/02-brand-identity/02-02-SUMMARY.md`
</output>
