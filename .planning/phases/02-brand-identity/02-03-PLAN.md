---
phase: 02-brand-identity
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/layout/Header.tsx
  - src/components/auth/SignInForm.tsx
  - src/components/auth/SignUpForm.tsx
  - src/components/auth/ResetPasswordForm.tsx
  - src/components/auth/TosAcceptance.tsx
  - src/components/auth/OnboardingWizard.tsx
  - src/components/pwa/InstallPrompt.tsx
  - src/app/~offline/page.tsx
  - src/app/page.tsx
  - public/icon-192x192.png
  - public/icon-512x512.png
autonomous: true
requirements:
  - BRAND-01

must_haves:
  truths:
    - "The Header wordmark uses Heebo 900 (font-black), text-xl, pink color (text-brand), tracking-tight — not the current font-bold text-lg white"
    - "The offline page wordmark matches the Header: font-weight 900, pink color, same sizing"
    - "ALL primary action buttons are pink (bg-brand) with near-black text (text-brand-foreground) — no white/neutral-100 primary buttons remain"
    - "Button hover states use the muted pink shade (hover:bg-brand-muted)"
    - "Selected option chips/toggles in OnboardingWizard use pink instead of white"
    - "The progress bar in OnboardingWizard uses pink fill (bg-brand) instead of white"
    - "The Install button in InstallPrompt uses pink background instead of white (#f5f5f5)"
    - "PWA icons show dark #0a0a0a background with pink #F472B6 accent — not the default Next.js icon"
    - "PWA icons are valid PNGs at 192x192 and 512x512 with sharp square edges (no rounded corners)"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Pink wordmark with Heebo 900, text-xl, tracking-tight"
      contains: "text-brand"
    - path: "src/components/auth/SignInForm.tsx"
      provides: "Pink primary button"
      contains: "bg-brand"
    - path: "src/components/auth/SignUpForm.tsx"
      provides: "Pink primary button"
      contains: "bg-brand"
    - path: "src/components/auth/ResetPasswordForm.tsx"
      provides: "Pink primary buttons (request + confirm)"
      contains: "bg-brand"
    - path: "src/components/auth/TosAcceptance.tsx"
      provides: "Pink accept button"
      contains: "bg-brand"
    - path: "src/components/auth/OnboardingWizard.tsx"
      provides: "Pink buttons, pink selected chips, pink progress bar"
      contains: "bg-brand"
    - path: "src/components/pwa/InstallPrompt.tsx"
      provides: "Pink install button (inline style #F472B6)"
      contains: "#F472B6"
    - path: "public/icon-192x192.png"
      provides: "Branded 192x192 PWA icon with dark bg and pink accent"
    - path: "public/icon-512x512.png"
      provides: "Branded 512x512 PWA icon with dark bg and pink accent"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "src/app/globals.css"
      via: "text-brand utility class referencing --color-brand CSS variable"
      pattern: "text-brand"
    - from: "src/components/auth/SignInForm.tsx"
      to: "src/app/globals.css"
      via: "bg-brand utility class referencing --color-brand CSS variable"
      pattern: "bg-brand"
---

<objective>
Apply pink visual identity across all interactive elements, style the brand wordmark, and generate branded PWA icons.

Purpose: Make the hot pink brand color (#F472B6) visible on every interactive surface. The wordmark in Header and offline page gets upgraded to Heebo 900 / text-xl / pink / tracking-tight. Every white/neutral-100 primary button switches to pink. Selected states, progress bars, and the install prompt all use pink. PWA icons get dark background with pink accent mark. After this plan, the brand identity is visually cohesive.

Output: Updated Header with pink wordmark, all auth buttons pink, progress bar pink, install button pink, offline wordmark pink, 2 branded PWA icon PNGs.
</objective>

<execution_context>
@C:/Users/somet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/somet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-brand-identity/02-RESEARCH.md
@.planning/phases/02-brand-identity/02-01-SUMMARY.md

Key source files (read before modifying):
@src/components/layout/Header.tsx
@src/components/auth/SignInForm.tsx
@src/components/auth/SignUpForm.tsx
@src/components/auth/ResetPasswordForm.tsx
@src/components/auth/TosAcceptance.tsx
@src/components/auth/OnboardingWizard.tsx
@src/components/pwa/InstallPrompt.tsx
@src/app/~offline/page.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pink wordmark in Header and offline page</name>
  <files>
    src/components/layout/Header.tsx
    src/app/~offline/page.tsx
  </files>
  <action>
Update the brand wordmark styling in two locations per user decisions.

**Header.tsx:**
Find the `<span>` that displays `{t("appName")}`. Change its classes from:
```
text-lg font-bold tracking-tight text-neutral-100
```
to:
```
text-xl font-black tracking-tight text-brand
```
- `text-xl`: bumped from text-lg for more presence
- `font-black`: Heebo 900 weight (not 700/bold) — heavy, stamp-like
- `text-brand`: pink color from the brand token (not white)
- `tracking-tight`: kept as-is

**Offline page (~offline/page.tsx):**
Find the branded header `<div>` that shows the wordmark text (currently "AcroYoga Academy" or "AcroHavura" if plan 02 ran first). Update its inline styles to match the Header wordmark:
- `fontWeight: 900` (was 700)
- `color: "#F472B6"` (was default text color / opacity 0.45 white) — pink, no opacity reduction
- `fontSize: "1.25rem"` (matches text-xl = 1.25rem)
- `letterSpacing: "-0.025em"` (matches tracking-tight)
- Remove `textTransform: "uppercase"` — wordmark is mixed-case "AcroHavura"
- Remove `opacity: 0.45` — pink should be full brightness

The Hebrew wordmark matches exactly per CONTEXT.md — same weight, color, size, tracking. Since Header uses i18n (appName key), it automatically renders אקרוחבורה in Hebrew with the same Tailwind classes. No separate Hebrew treatment needed in Header.

The offline page is English-only (locale-agnostic) so it always shows "AcroHavura".
  </action>
  <verify>
Run: `grep "font-black" src/components/layout/Header.tsx` — must match.
Run: `grep "text-brand" src/components/layout/Header.tsx` — must match.
Run: `grep "text-xl" src/components/layout/Header.tsx` — must match.
Run: `grep "900" src/app/~offline/page.tsx` — must match (fontWeight: 900).
Run: `grep "F472B6" src/app/~offline/page.tsx` — must match (pink color).
  </verify>
  <done>Header wordmark is Heebo 900, text-xl, pink (text-brand), tracking-tight. Offline page wordmark matches: weight 900, pink #F472B6, 1.25rem, tight tracking, no uppercase, no opacity.</done>
</task>

<task type="auto">
  <name>Task 2: Switch all white/neutral-100 buttons to pink</name>
  <files>
    src/components/auth/SignInForm.tsx
    src/components/auth/SignUpForm.tsx
    src/components/auth/ResetPasswordForm.tsx
    src/components/auth/TosAcceptance.tsx
    src/components/auth/OnboardingWizard.tsx
    src/components/pwa/InstallPrompt.tsx
    src/app/page.tsx
  </files>
  <action>
Replace all white/neutral-100 primary button backgrounds with pink brand color across every component. This is the single most impactful visual change — every interactive action surface becomes hot pink.

**Pattern: Primary action buttons (Tailwind classes)**
These files all use the same button pattern:
```
bg-neutral-100 ... text-neutral-950 ... hover:bg-white
```
Replace with:
```
bg-brand ... text-brand-foreground ... hover:bg-brand-muted
```

Files with this exact pattern (find and replace):
1. **SignInForm.tsx** — 1 button ("Sign In")
2. **SignUpForm.tsx** — 1 button ("Create Account")
3. **ResetPasswordForm.tsx** — 2 buttons ("Send Reset Link" and "Set New Password")
4. **TosAcceptance.tsx** — 1 button ("I Accept")
5. **OnboardingWizard.tsx** — 2 "Next" / "Save" buttons (Step 1 Next, Step 2 Save)

For each of these, the full class replacement is:
- `bg-neutral-100` -> `bg-brand`
- `text-neutral-950` -> `text-brand-foreground`
- `hover:bg-white` -> `hover:bg-brand-muted`

**OnboardingWizard.tsx — additional pink surfaces:**

a) **Selected option chips** (city role buttons, level buttons): Currently use conditional classes:
   ```
   "border-neutral-100 bg-neutral-100 text-neutral-950"
   ```
   Replace the selected state with:
   ```
   "border-brand bg-brand text-brand-foreground"
   ```
   There are 2 occurrences of this pattern (Step 1 role chips and Step 2 level chips).

b) **Progress bar fill**: Currently uses `bg-neutral-100`. Replace with `bg-brand`.

**InstallPrompt.tsx — inline style button:**
The Install button uses inline styles (not Tailwind). Change:
- `backgroundColor: "#f5f5f5"` -> `backgroundColor: "#F472B6"`
- `color: "#0a0a0a"` -> `color: "#0a0a0a"` (keep as-is, black text on pink)

**page.tsx (root landing page):**
If it has `bg-white dark:bg-black`, change to just `bg-background` (uses the dark background variable). This is a minor cleanup — the root page should not have a white background.

**Do NOT change secondary/ghost buttons** (like "Skip", "Decline", sign-out button, dismiss X). Those stay as subtle outlined/transparent buttons. Only primary action buttons become pink.
  </action>
  <verify>
Run: `grep -r "bg-neutral-100" src/components/auth/ src/components/pwa/InstallPrompt.tsx` — progress bar hit is acceptable ONLY if it now says bg-brand. If any primary button still has bg-neutral-100, the task is incomplete.

Specifically verify:
- `grep "bg-brand" src/components/auth/SignInForm.tsx` — must match
- `grep "bg-brand" src/components/auth/SignUpForm.tsx` — must match
- `grep "bg-brand" src/components/auth/ResetPasswordForm.tsx` — must match (2 buttons)
- `grep "bg-brand" src/components/auth/TosAcceptance.tsx` — must match
- `grep "bg-brand" src/components/auth/OnboardingWizard.tsx` — must match (buttons + chips + progress bar)
- `grep "F472B6" src/components/pwa/InstallPrompt.tsx` — must match (inline style)
  </verify>
  <done>All primary action buttons across auth forms, TOS acceptance, onboarding wizard, and install prompt now use hot pink (bg-brand / #F472B6) with near-black text and muted pink hover. Selected chips and progress bar in onboarding are pink. No white/neutral-100 primary buttons remain. Secondary buttons (Skip, Decline, sign-out, dismiss) are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Generate branded placeholder PWA icons</name>
  <files>
    public/icon-192x192.png
    public/icon-512x512.png
  </files>
  <action>
Generate two branded placeholder PWA icons to replace the default Next.js icons. These are functional placeholders — real designed icons are deferred to Phase 10.

**Design spec (per CONTEXT.md):**
- Background: #0a0a0a (dark, matches app theme)
- Sharp square edges — NO rounded corners (brutalist)
- Pink #F472B6 accent element
- Claude's discretion on content: use bold geometric pink element on dark background

**Recommended design:** A thick pink (#F472B6) border/frame inset from the edges, leaving a dark center. Simple, brutalist, unmistakable. The frame thickness should be ~15% of icon size (e.g., 28px for 192, 76px for 512).

**Generation approach:** Use a Node.js script that creates valid PNG files programmatically. The Phase 1 plan (01-04) already demonstrated this pattern — generating solid-color PNGs with raw zlib/CRC32 encoding without needing canvas or sharp.

For the frame design, generate the image pixel-by-pixel:
- If pixel is within the border region (within N pixels of any edge): pink (#F4, #72, #B6)
- Otherwise: dark (#0A, #0A, #0A)
- Alpha channel: 0xFF (fully opaque)

Write a temporary Node.js script (e.g., `/tmp/gen-icons.mjs`), run it, then delete the script. The output PNGs go to `public/icon-192x192.png` and `public/icon-512x512.png`, overwriting existing files.

The icons MUST be valid PNG files that satisfy the PWA manifest `icons` array. Verify with `file` command.
  </action>
  <verify>
Run: `file public/icon-192x192.png` — must show "PNG image data, 192 x 192".
Run: `file public/icon-512x512.png` — must show "PNG image data, 512 x 512".
Both files must NOT be the default Next.js black-and-white "N" icon (size should differ from the stock ~1KB Next.js placeholder).
  </verify>
  <done>Two branded PWA icons (192x192 and 512x512) at public/icon-192x192.png and public/icon-512x512.png. Dark #0a0a0a background with thick pink #F472B6 frame/border — sharp square edges, brutalist, no rounded corners. Valid PNGs referenced by manifest.</done>
</task>

</tasks>

<verification>
1. `grep "font-black" src/components/layout/Header.tsx` confirms Heebo 900 weight
2. `grep "text-brand" src/components/layout/Header.tsx` confirms pink wordmark
3. `grep "bg-brand" src/components/auth/SignInForm.tsx` confirms pink button
4. `grep "bg-brand" src/components/auth/SignUpForm.tsx` confirms pink button
5. `grep "bg-brand" src/components/auth/OnboardingWizard.tsx` confirms pink buttons/chips/progress
6. `grep "F472B6" src/components/pwa/InstallPrompt.tsx` confirms pink install button
7. `grep "F472B6" src/app/~offline/page.tsx` confirms pink offline wordmark
8. `file public/icon-192x192.png` confirms valid PNG 192x192
9. `file public/icon-512x512.png` confirms valid PNG 512x512
10. `npx next build` succeeds without errors
</verification>

<success_criteria>
- Header wordmark: Heebo 900, text-xl, pink (text-brand), tracking-tight
- Offline wordmark: weight 900, pink #F472B6, 1.25rem, tight tracking
- All primary action buttons use bg-brand (pink) with text-brand-foreground and hover:bg-brand-muted
- Selected chips in OnboardingWizard use pink (bg-brand)
- Progress bar in OnboardingWizard uses pink (bg-brand)
- Install button in InstallPrompt uses pink (#F472B6) inline style
- No white/neutral-100 primary buttons remain in the codebase
- PWA icons are branded dark+pink placeholders (192x192 and 512x512)
- Zero references to old button pattern (bg-neutral-100 as primary action)
</success_criteria>

<output>
After completion, create `.planning/phases/02-brand-identity/02-03-SUMMARY.md`
</output>
