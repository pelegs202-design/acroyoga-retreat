---
phase: 04-jam-board-messaging
plan: 05
type: execute
wave: 3
depends_on:
  - 04-03
files_modified:
  - src/app/[locale]/(app)/messages/page.tsx
  - src/app/[locale]/(app)/messages/[conversationId]/page.tsx
  - src/components/messages/ConversationList.tsx
  - src/components/messages/ChatThread.tsx
  - src/components/messages/MessageBubble.tsx
  - src/components/messages/NewMessagePicker.tsx
  - src/components/layout/Header.tsx
  - src/app/api/reviews/route.ts
  - src/app/[locale]/(app)/members/[userId]/page.tsx
  - messages/en.json
  - messages/he.json
autonomous: true
requirements:
  - COMM-02

must_haves:
  truths:
    - "User can see a list of their conversations with last message preview and unread badge"
    - "User can tap a conversation to open a full-screen chat thread"
    - "User can type and send a message that appears instantly via Ably real-time"
    - "Messages persist in the database and survive page refresh"
    - "User can start a new conversation from 'New message' button (member search picker)"
    - "User can click 'Message' button on a member's profile to start a DM"
    - "Header shows unread message badge (pink dot or count) when there are unread messages"
    - "No read receipts shown — only sent timestamps"
    - "canReview() stub in reviews API is replaced with real jam attendance check"
  artifacts:
    - path: "src/app/[locale]/(app)/messages/page.tsx"
      provides: "Conversation list page"
      contains: "ConversationList"
    - path: "src/app/[locale]/(app)/messages/[conversationId]/page.tsx"
      provides: "Full-screen chat thread page"
      contains: "ChatThread"
    - path: "src/components/messages/ConversationList.tsx"
      provides: "Conversation list with unread dots and new message button"
      contains: "conversations"
    - path: "src/components/messages/ChatThread.tsx"
      provides: "Real-time chat with useChannel hook"
      contains: "useChannel"
    - path: "src/components/messages/MessageBubble.tsx"
      provides: "Individual message bubble with sent timestamp"
      contains: "sentAt"
    - path: "src/components/messages/NewMessagePicker.tsx"
      provides: "Member search to start new conversation"
      contains: "search"
    - path: "src/components/layout/Header.tsx"
      provides: "Updated header with unread badge and nav links"
      contains: "unreadCount"
    - path: "src/app/api/reviews/route.ts"
      provides: "canReview() replaced with real jam attendance check"
      contains: "jamAttendees"
  key_links:
    - from: "src/components/messages/ChatThread.tsx"
      to: "ably/react"
      via: "useChannel hook for real-time messages"
      pattern: "useChannel"
    - from: "src/components/messages/ChatThread.tsx"
      to: "/api/messages/[conversationId]"
      via: "fetch for history + POST for send"
      pattern: "fetch.*api/messages"
    - from: "src/components/layout/Header.tsx"
      to: "/api/messages"
      via: "fetch for unread count"
      pattern: "unread"
    - from: "src/app/api/reviews/route.ts"
      to: "jamAttendees"
      via: "Drizzle query for shared jam attendance"
      pattern: "jamAttendees"
---

<objective>
Messaging UI + Header unread badge + canReview() fix + navigation links.

Purpose: Build the user-facing messaging interface — conversation list with unread badges, full-screen iMessage-style chat with real-time Ably delivery, new message picker, profile "Message" button — and wire the unread badge into the Header. Also replace the canReview() stub with real jam attendance logic now that jamAttendees table exists.
Output: Complete messaging experience at /messages with real-time chat, plus unread badge in header, plus canReview fix.
</objective>

<execution_context>
@C:/Users/somet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/somet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-jam-board-messaging/04-CONTEXT.md
@.planning/phases/04-jam-board-messaging/04-01-SUMMARY.md
@.planning/phases/04-jam-board-messaging/04-03-SUMMARY.md
@src/components/layout/Header.tsx
@src/app/api/reviews/route.ts
@src/app/[locale]/(app)/members/[userId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create messaging pages and chat components</name>
  <files>src/app/[locale]/(app)/messages/page.tsx, src/app/[locale]/(app)/messages/[conversationId]/page.tsx, src/components/messages/ConversationList.tsx, src/components/messages/ChatThread.tsx, src/components/messages/MessageBubble.tsx, src/components/messages/NewMessagePicker.tsx</files>
  <action>
**1. Create `src/app/[locale]/(app)/messages/page.tsx`** — Server component:
- Auth guard via `getAuthSession()`
- `setRequestLocale(locale)`
- Render `<ConversationList locale={locale} userId={session.user.id} />`

**2. Create `src/components/messages/ConversationList.tsx`** — 'use client':
- Fetch conversations from GET /api/messages on mount
- Display list of conversation cards:
  - Other user's avatar (small circle) + name
  - Last message preview (truncated to ~50 chars)
  - Relative timestamp for last message (e.g., "2h ago", "Yesterday")
  - Unread dot (pink bg-brand circle) if unreadCount > 0
- Tap a conversation: navigate to `/messages/{conversationId}`
- **"New message" button** at top: opens NewMessagePicker inline or navigates to picker view
- Empty state: "No conversations yet" message
- Dark styling: bg-neutral-900 cards, border-neutral-800

**3. Create `src/components/messages/NewMessagePicker.tsx`** — 'use client':
- Search input that filters members by name (ilike pattern, same as members page search)
- Fetch from existing members API or a lightweight endpoint
- Display matching members as tappable rows (avatar + name)
- On tap: navigate to `/messages/new?with={userId}` — the chat thread page handles lazy conversation creation
- If navigated from profile "Message" button, `with` param is pre-filled

**4. Create `src/app/[locale]/(app)/messages/[conversationId]/page.tsx`** — Server component wrapper:
- Auth guard
- `setRequestLocale(locale)`
- Determine if this is a new conversation (conversationId === 'new' with `?with=userId` query param) or existing
- Wrap in `<AblyClientProvider>` (import from `@/components/messages/AblyClientProvider`)
- Render `<ChatThread conversationId={conversationId} userId={session.user.id} locale={locale} recipientId={searchParams.with} />`

**5. Create `src/components/messages/ChatThread.tsx`** — 'use client':
- Props: conversationId (or 'new'), userId, locale, recipientId (optional, for new conversations)
- **On mount:** Fetch message history from GET /api/messages/{conversationId}?before=... (unless new conversation)
- **Real-time with Ably:**
  - Compute channel name using `dmChannel(userId, otherUserId)` from `@/lib/dm-channel`
  - Use `useChannel` from `ably/react`:
    ```
    const { publish } = useChannel(channelName, (message) => {
      // Append incoming message to local state (avoid duplicates — check message.id)
      setMessages(prev => [...prev, message.data]);
    });
    ```
- **Send message flow:**
  1. POST to /api/messages/{conversationId} (or /api/messages/new?recipientId=X for first message)
  2. On success, publish to Ably channel: `await publish('message', { id, senderId: userId, text, sentAt })`
  3. Append to local state optimistically (or wait for Ably echo — either works since sender adds immediately)
  4. If this was a new conversation, update conversationId from the API response and update URL
- **Layout:** Full-screen mobile-first iMessage-style:
  - Messages take full viewport height minus header and input bar
  - Scroll to bottom on mount and new message
  - Messages area scrollable (overflow-y-auto)
  - Input bar fixed at bottom: text input + send button
  - Send button: pink bg-brand
- **Message rendering:** Map messages array to MessageBubble components

**6. Create `src/components/messages/MessageBubble.tsx`** — pure component:
- Props: message object (senderId, text, sentAt), isOwn (boolean)
- Own messages: aligned right, bg-brand/20 or bg-brand text-brand-foreground
- Other's messages: aligned left, bg-neutral-800
- Show sent timestamp below message (small text, neutral-500) — NO read receipts per user decision
- Use CSS logical properties for RTL support (end/start alignment instead of left/right)
  </action>
  <verify>Run `npx tsc --noEmit` — no errors. Verify ChatThread uses useChannel from ably/react. Verify MessageBubble shows only sentAt (no read receipts). Verify ConversationList shows unread dots.</verify>
  <done>Full messaging UI: conversation list at /messages, full-screen chat at /messages/[id], real-time via Ably, new message picker, message bubbles with timestamps only.</done>
</task>

<task type="auto">
  <name>Task 2: Update Header with nav links and unread badge, fix canReview, add Message button to profiles</name>
  <files>src/components/layout/Header.tsx, src/app/api/reviews/route.ts, src/app/[locale]/(app)/members/[userId]/page.tsx, messages/en.json, messages/he.json</files>
  <action>
**1. Update `src/components/layout/Header.tsx`:**
- Add navigation links for authenticated users (between app name and trailing controls):
  - "Jams" link → /jams (use Link from @/i18n/navigation)
  - "Messages" link → /messages
  - Use `useTranslations('common')` with keys `nav.jams` and `nav.messages`
- **Unread badge on Messages link:**
  - Fetch unread count from GET /api/messages on mount (extract total unread from conversations response)
  - OR create a lightweight GET /api/messages/unread endpoint that returns just the count
  - If unreadCount > 0, show a pink dot (w-2 h-2 rounded-full bg-brand) or count badge next to "Messages" text
  - Style: `relative` parent, absolute-positioned dot at top-end of the link
- Keep existing header structure intact — app name on leading edge, auth controls on trailing edge
- Nav links sit between app name and auth controls, centered or after app name

**2. Replace canReview() stub in `src/app/api/reviews/route.ts`:**
- Replace the stub block (lines 68-74: `const canReview = true;`) with real jam attendance check:
  ```
  // Find jams where BOTH the reviewer and reviewee attended (status = 'confirmed')
  // Pattern: find jam IDs where current user is confirmed, then check if reviewee is confirmed in any of those jams
  const myJamIds = db
    .select({ jamId: jamAttendees.jamId })
    .from(jamAttendees)
    .where(and(
      eq(jamAttendees.userId, session.user.id),
      eq(jamAttendees.status, 'confirmed'),
    ));

  const [sharedJam] = await db
    .select({ id: jamAttendees.id })
    .from(jamAttendees)
    .where(and(
      eq(jamAttendees.userId, revieweeId),
      eq(jamAttendees.status, 'confirmed'),
      inArray(jamAttendees.jamId, myJamIds),
    ))
    .limit(1);

  const canReview = !!sharedJam;
  ```
- Import `jamAttendees` from schema
- Import `inArray` from 'drizzle-orm'
- Keep the 403 response for !canReview unchanged

**3. Add "Message" button to member profile page:**
- In `src/app/[locale]/(app)/members/[userId]/page.tsx`:
  - Add a "Message" button on non-own profiles (same visibility logic as ReviewForm — shown for other users, hidden on own profile)
  - Button navigates to `/messages/new?with={userId}` — does NOT create a conversation row (lazy creation per research anti-pattern guidance)
  - Style: outlined button, border-brand text-brand, placed near the top of the profile (in ProfileHero area or just below)
  - Use i18n key: `messages.sendMessage` / "Message" / "שלח הודעה"

**4. Add messaging i18n strings** to `messages/en.json` and `messages/he.json`:
Add a `"messages"` namespace with keys:
- `title`: "Messages" / "הודעות"
- `newMessage`: "New message" / "הודעה חדשה"
- `sendMessage`: "Message" / "שלח הודעה"
- `noConversations`: "No conversations yet" / "אין שיחות עדיין"
- `typeMessage`: "Type a message..." / "כתוב הודעה..."
- `send`: "Send" / "שלח"
- `searchMembers`: "Search members..." / "חפש חברים..."
- `justNow`: "Just now" / "עכשיו"
  </action>
  <verify>Run `npx tsc --noEmit` — no errors. Verify Header shows Jams + Messages nav links when authenticated. Verify unread badge appears on Messages link. Verify canReview uses inArray + jamAttendees subquery (not the const=true stub). Verify "Message" button on non-own profiles navigates to /messages/new?with=userId.</verify>
  <done>Header has Jams/Messages nav links with unread badge. canReview() uses real jam attendance check via jamAttendees. Member profiles have "Message" button for non-own profiles. All messaging i18n strings in English and Hebrew.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- /messages page shows conversation list with unread indicators
- /messages/[id] shows full-screen chat with real-time message delivery
- New message picker allows searching and selecting members
- Header shows "Jams" and "Messages" nav links
- Unread badge visible on Messages link when unread messages exist
- "Message" button on member profiles navigates to /messages/new?with=userId
- canReview() stub replaced with real jam attendance check
- All text bilingual English/Hebrew
</verification>

<success_criteria>
Complete messaging UI with real-time Ably delivery, conversation list with unread badges, full-screen chat thread, new message picker, profile Message button. Header updated with navigation links and unread badge. canReview() stub replaced with production jam attendance check. All strings bilingual.
</success_criteria>

<output>
After completion, create `.planning/phases/04-jam-board-messaging/04-05-SUMMARY.md`
</output>
