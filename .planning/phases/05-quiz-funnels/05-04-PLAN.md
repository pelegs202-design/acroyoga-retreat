---
phase: 05-quiz-funnels
plan: 04
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - src/app/[locale]/(app)/quiz/challenge/results/page.tsx
  - src/components/quiz/QuizLoader.tsx
  - src/components/quiz/QuizRadarChart.tsx
  - src/components/quiz/QuizResultsPage.tsx
  - src/app/api/quiz/results/[sessionId]/route.ts
  - messages/en.json
  - messages/he.json
autonomous: true
requirements:
  - QUIZ-03
  - QUIZ-04

must_haves:
  truths:
    - "User sees a fake loading animation for ~2.5 seconds before results appear"
    - "Results page shows personalized archetype name, description, and strengths"
    - "Radar chart displays current level vs potential level with two overlapping shapes"
    - "Results page addresses user fears based on their quiz answers"
    - "Price anchoring shows 299 NIS crossed-out from 499 NIS"
    - "Testimonials section shows real-style social proof"
    - "FAQ section answers common questions"
    - "Results page has a shareable URL via session ID"
    - "CTA button exists (placeholder link to Phase 6 payment)"
  artifacts:
    - path: "src/components/quiz/QuizLoader.tsx"
      provides: "Fake loading animation with 3 phases"
      min_lines: 30
    - path: "src/components/quiz/QuizRadarChart.tsx"
      provides: "Recharts radar with current vs potential"
      min_lines: 40
    - path: "src/components/quiz/QuizResultsPage.tsx"
      provides: "Full results composition with CTA"
      min_lines: 150
    - path: "src/app/[locale]/(app)/quiz/challenge/results/page.tsx"
      provides: "Results route page"
      min_lines: 20
    - path: "src/app/api/quiz/results/[sessionId]/route.ts"
      provides: "GET endpoint to fetch results by session"
      exports: ["GET"]
  key_links:
    - from: "src/components/quiz/QuizResultsPage.tsx"
      to: "src/components/quiz/QuizRadarChart.tsx"
      via: "renders radar chart with archetype radarData"
      pattern: "QuizRadarChart"
    - from: "src/app/[locale]/(app)/quiz/challenge/results/page.tsx"
      to: "src/app/api/quiz/results/[sessionId]/route.ts"
      via: "fetches result data by session ID"
      pattern: "api/quiz/results"
    - from: "src/components/quiz/QuizResultsPage.tsx"
      to: "src/lib/quiz/result-calculator.ts"
      via: "uses archetype data for display"
      pattern: "ResultArchetype"
---

<objective>
Build the challenge quiz results page with fake loading animation, personalized archetype display, radar chart (current vs potential), fear addressing, price anchoring CTA, testimonials, FAQ, and shareable URL support.

Purpose: The results page is the conversion centerpiece — it must feel personalized, build desire, address objections, and lead to the payment CTA.
Output: Results page route, 3 quiz components (loader, radar chart, results composition), results API endpoint
</objective>

<execution_context>
@C:/Users/somet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/somet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-quiz-funnels/05-01-SUMMARY.md
@.planning/phases/05-quiz-funnels/05-02-SUMMARY.md
@src/lib/db/schema.ts
@messages/en.json
@messages/he.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuizLoader + QuizRadarChart + results API</name>
  <files>
    src/components/quiz/QuizLoader.tsx
    src/components/quiz/QuizRadarChart.tsx
    src/app/api/quiz/results/[sessionId]/route.ts
  </files>
  <action>
**QuizLoader.tsx** — `"use client"` component. Props: `onComplete: () => void`, `locale: string`.

Three-phase fake loading animation, total 2.5 seconds:
1. Phase 1 (0–0.8s): "Analyzing your answers..." / "...מנתחים את התשובות שלך"
2. Phase 2 (0.8–1.8s): "Calculating your acro profile..." / "...מחשבים את פרופיל האקרו שלך"
3. Phase 3 (1.8–2.5s): "Preparing your personal recommendations..." / "...מכינים את ההמלצות האישיות שלך"

Use `useState` for current phase, `useEffect` with `setTimeout` to advance phases. Call `onComplete()` when done.

Visual: Center the text vertically. Use Framer Motion `AnimatePresence` to fade/slide each phase text. Add a circular progress indicator — use a `motion.div` with a rotating border or a simple SVG circle with animating `strokeDashoffset`. Background: full-screen dark overlay.

**QuizRadarChart.tsx** — `"use client"` component. Props: `radarData: Array<{ axis: string; axisHe: string; current: number; potential: number }>`, `locale: string`.

Use Recharts `RadarChart`:
```tsx
import { RadarChart, Radar, PolarGrid, PolarAngleAxis, ResponsiveContainer, Legend } from 'recharts';
```

Transform radarData to use locale-appropriate axis label (axis for en, axisHe for he).

Render `ResponsiveContainer` (width 100%, height 300px) wrapping `RadarChart`:
- `PolarGrid` with stroke="#333"
- `PolarAngleAxis` dataKey="axis" with tick fill="#aaa" fontSize 12
- Radar 1 (current): name="Current"/"נוכחי", dataKey="current", stroke="#555", fill="#555", fillOpacity 0.3
- Radar 2 (potential): name="After 30 Days"/"אחרי 30 יום", dataKey="potential", stroke="#F472B6", fill="#F472B6", fillOpacity 0.4
- `Legend` at bottom

**api/quiz/results/[sessionId]/route.ts** — GET endpoint:
- Extract `sessionId` from params
- Query `quizLeads` table where sessionId matches
- If not found, return 404 `{ error: "Session not found" }`
- Parse the JSON `answers` field
- Run `calculateResult(answers)` from result-calculator to get archetype
- Run `getPersonalizedFears(answers)` for additional fears
- Return `{ ok: true, lead: { name, city, quizType }, result: archetype, personalizedFears }`
- No auth required (results are shareable)
  </action>
  <verify>
- QuizLoader renders 3 phases and calls onComplete after ~2.5s
- QuizRadarChart renders a Recharts RadarChart with two Radar layers
- GET /api/quiz/results/[sessionId] returns result data for a valid session
- GET /api/quiz/results/nonexistent returns 404
  </verify>
  <done>Fake loader shows 3 analysis phases over 2.5s; radar chart displays current (gray) vs potential (pink) with 5 axes; results API fetches lead from DB and computes archetype on the fly</done>
</task>

<task type="auto">
  <name>Task 2: QuizResultsPage composition + results route page + i18n</name>
  <files>
    src/components/quiz/QuizResultsPage.tsx
    src/app/[locale]/(app)/quiz/challenge/results/page.tsx
    messages/en.json
    messages/he.json
  </files>
  <action>
**Add i18n strings** to `messages/en.json` and `messages/he.json` under `quiz.results`:

English:
```json
"results": {
  "title": "Your Acro Profile",
  "youAre": "You are",
  "currentVsPotential": "Where You Are → Where You Could Be",
  "strengths": "Your Strengths",
  "dontWorry": "We've Got You",
  "testimonials": "What Our Students Say",
  "ctaTitle": "Join the 30-Day Challenge",
  "earlyBird": "Early Bird Price",
  "regularPrice": "Regular price",
  "currency": "₪",
  "spotsLeft": "Only {count} spots left in the next group",
  "nextStart": "Next group starts {date}",
  "joinNow": "I Want to Join Now",
  "comingSoon": "Payment page coming soon",
  "share": "Share Your Results",
  "faq": "Frequently Asked Questions",
  "faqItems": [
    { "q": "Do I need experience?", "a": "Not at all! 80% of our students start as complete beginners. Our classes are designed to welcome everyone." },
    { "q": "Do I need to bring a partner?", "a": "No partner needed! We pair everyone up in class. Many students come alone and meet their regular practice partners here." },
    { "q": "What should I wear?", "a": "Comfortable athletic clothes that you can move in. Avoid zippers and buttons that could scratch your partner." },
    { "q": "How fit do I need to be?", "a": "Any fitness level works. AcroYoga builds strength and flexibility progressively — you'll surprise yourself by week 2." },
    { "q": "What if I'm afraid of heights?", "a": "We start low to the ground and build up gradually. You'll always feel safe and supported by your base and spotter." }
  ]
}
```

Hebrew:
```json
"results": {
  "title": "פרופיל האקרו שלך",
  "youAre": "אתם",
  "currentVsPotential": "איפה אתם → איפה אתם יכולים להיות",
  "strengths": "החוזקות שלכם",
  "dontWorry": "אנחנו כאן בשבילכם",
  "testimonials": "מה התלמידים שלנו אומרים",
  "ctaTitle": "הצטרפו לאתגר 30 יום",
  "earlyBird": "מחיר Early Bird",
  "regularPrice": "מחיר רגיל",
  "currency": "₪",
  "spotsLeft": "נותרו רק {count} מקומות בקבוצה הבאה",
  "nextStart": "הקבוצה הבאה מתחילה ב-{date}",
  "joinNow": "אני רוצה להצטרף עכשיו",
  "comingSoon": "עמוד תשלום בקרוב",
  "share": "שתפו את התוצאות שלכם",
  "faq": "שאלות נפוצות",
  "faqItems": [
    { "q": "צריך ניסיון?", "a": "בכלל לא! 80% מהתלמידים שלנו מתחילים כמתחילים מוחלטים. השיעורים מתוכננים לקבל את כולם." },
    { "q": "צריך להגיע עם פרטנר?", "a": "לא צריך! אנחנו מזווגים את כולם בשיעור. הרבה תלמידים מגיעים לבד ומוצאים פרטנרים קבועים כאן." },
    { "q": "מה ללבוש?", "a": "בגדי ספורט נוחים שאפשר לזוז בהם. הימנעו מרוכסנים וכפתורים שעלולים לשרוט את הפרטנר." },
    { "q": "כמה צריך להיות בכושר?", "a": "כל רמת כושר מתאימה. אקרויוגה בונה כוח וגמישות בהדרגה — תפתיעו את עצמכם כבר בשבוע השני." },
    { "q": "מה אם אני פוחד מגובה?", "a": "מתחילים נמוך ומתקדמים בהדרגה. תמיד תרגישו בטוחים ונתמכים על ידי הבייס והספוטר." }
  ]
}
```

**QuizResultsPage.tsx** — `"use client"` component. Props: `result: ResultArchetype`, `personalizedFears: Array<{ en: string; he: string }>`, `leadName: string`, `locale: string`.

Layout sections (top to bottom):

1. **Archetype Header** — Large archetype name (result.name[locale]), tagline below, description paragraph. Use motion.div fadeIn animation.

2. **Radar Chart** — Render `QuizRadarChart` with result.radarData. Section title: "Where You Are → Where You Could Be". Framed in a bordered card.

3. **Strengths** — List result.strengths with checkmark icons, green text.

4. **Fear Addressing** — Section title "We've Got You". List result.fears + personalizedFears. Each as a reassuring card with a ✅ icon. This directly addresses the user's concerns based on their quiz answers.

5. **Testimonials** — 4 testimonials (hardcoded in the component, bilingual):
   - Testimonial 1 (beginner): "הגעתי בלי שום ניסיון, חשבתי שלא אצליח. אחרי השיעור הראשון עם שי הבנתי שזה בדיוק בשבילי. היום אני יכולה לעוף." — מיטל, תל אביב / "I came with zero experience, thought I'd never manage. After my first class with Shai I knew this was for me. Today I can fly." — Mital, Tel Aviv
   - Testimonial 2 (social): "חיפשתי ספורט שהוא גם חברתי. מצאתי קהילה שלמה. שי יצר מרחב שבו כולם מרגישים שייכים." — דניאל, כפר סבא / "I was looking for a sport that's also social. Found a whole community. Shai created a space where everyone feels they belong." — Daniel, Kfar Saba
   - Testimonial 3 (fitness): "תוך חודש ראיתי שינוי בכוח וביציבה שלי. לא ידעתי שיש ספורט כזה — מאתגר, כייפי, וחברתי." — אלה, תל אביב / "Within a month I saw changes in my strength and posture. Didn't know a sport like this existed — challenging, fun, and social." — Ella, Tel Aviv
   - Testimonial 4 (trust): "תרגלתי יוגה שנים, אבל אקרויוגה פתח לי עולם חדש של אמון ושיתוף פעולה. שי מלמד בסבלנות ובדיוק." — יואב, תל אביב / "I practiced yoga for years, but acroyoga opened a new world of trust and collaboration. Shai teaches with patience and precision." — Yoav, Tel Aviv

6. **Price CTA Section** — Card with:
   - Title: "Join the 30-Day Challenge"
   - Early Bird price: **299 ₪** in large bold brand-colored text
   - Regular price: ~~499 ₪~~ in muted text with line-through
   - Urgency: "Only 4 spots left in the next group" (display-only count, not DB-backed per Claude's discretion — simpler and sufficient for MVP)
   - Next start date: "Next group starts May 15, 2026" (hardcoded for now)
   - CTA button: Large, full-width, bg-brand text-brand-foreground, text "I Want to Join Now" / "אני רוצה להצטרף עכשיו". Link to `#coming-soon` for now (Phase 6 payment). Show a small "Payment page coming soon" note below.

7. **FAQ Section** — Accordion-style (each question expandable). Use local state with `useState` for open/closed. Use translations from `quiz.results.faqItems`. Style: dark cards, border-neutral-800, smooth height animation.

8. **Share Button** — At the bottom. Copies the current URL to clipboard (uses `navigator.clipboard.writeText`). Show "Copied!" toast for 2 seconds. Text: "Share Your Results" / "שתפו את התוצאות שלכם".

**quiz/challenge/results/page.tsx** — Server Component wrapper:
- Read `searchParams.session` (the session ID)
- If no session param, redirect to `/quiz`
- Render a `ChallengeResultsFlow` client component that:
  1. On mount, tries localStorage first for result data (for the original quiz taker)
  2. If no localStorage, fetches `GET /api/quiz/results/{sessionId}`
  3. Shows QuizLoader first (2.5s fake animation)
  4. After loader completes, shows QuizResultsPage with fetched/cached data
  5. If API returns 404, show "Results not found" with link back to /quiz
- Generate metadata with the archetype name for social sharing: `title: "I'm [archetype] | AcroHavura"` (server-side, use a generic title since archetype isn't known at SSR time: "Your Acro Profile | AcroHavura")
  </action>
  <verify>
- `/quiz/challenge/results?session=XXX` shows the loader then results
- Results page has all 7 sections: archetype, radar, strengths, fears, testimonials, price CTA, FAQ
- Price shows 299 with crossed-out 499
- FAQ items expand/collapse
- Share button copies URL to clipboard
- Missing session redirects to /quiz
- i18n strings added to both language files
  </verify>
  <done>Results page shows personalized archetype with radar chart, strengths, fear addressing, testimonials, price anchoring (299/~~499~~), urgency elements, FAQ accordion, and shareable URL. Fake loader plays for 2.5s before reveal.</done>
</task>

</tasks>

<verification>
- Complete flow: /quiz → /quiz/challenge → complete quiz → /quiz/challenge/results?session=X shows full results
- Radar chart renders with two shapes (current gray, potential pink)
- Fake loader shows 3 phases over ~2.5 seconds
- Price section shows 299 ₪ with crossed-out 499 ₪
- FAQ accordion works (expand/collapse)
- Shareable URL: visiting results?session=X in new browser fetches from API
- All text displays correctly in both English and Hebrew
</verification>

<success_criteria>
The results page converts quiz takers into challenge prospects. A visitor sees their personalized archetype, understands their potential via the radar chart, has their fears addressed, reads social proof, sees the price with early bird anchoring, and has a clear CTA to join. The page is shareable via URL and works in both languages.
</success_criteria>

<output>
After completion, create `.planning/phases/05-quiz-funnels/05-04-SUMMARY.md`
</output>
