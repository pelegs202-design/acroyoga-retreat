---
phase: 01-foundation-auth
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - next.config.ts
  - app/sw.ts
  - app/manifest.ts
  - app/~offline/page.tsx
  - src/components/pwa/InstallPrompt.tsx
  - src/components/pwa/IosBanner.tsx
  - public/icon-192x192.png
  - public/icon-512x512.png
autonomous: true
requirements:
  - FOUND-03

must_haves:
  truths:
    - "App can be installed to a mobile home screen via browser install prompt"
    - "PWA manifest is accessible at /manifest.json with correct dark theme colors"
    - "Service worker registers and caches the app shell"
    - "Offline fallback page shows branded message when network is unavailable"
    - "Install prompt appears only after 2nd visit (not first)"
    - "iOS users see a static banner with share-button instructions"
  artifacts:
    - path: "app/manifest.ts"
      provides: "PWA web manifest with dark brutalist theme"
      contains: "background_color"
    - path: "app/sw.ts"
      provides: "Serwist service worker with offline fallback"
      contains: "Serwist"
    - path: "next.config.ts"
      provides: "Next.js config with Serwist plugin"
      contains: "withSerwist"
    - path: "app/~offline/page.tsx"
      provides: "Offline fallback page"
    - path: "src/components/pwa/InstallPrompt.tsx"
      provides: "Deferred install prompt (shows on 2nd visit)"
      contains: "beforeinstallprompt"
  key_links:
    - from: "next.config.ts"
      to: "app/sw.ts"
      via: "Serwist swSrc config"
      pattern: "swSrc.*sw\\.ts"
    - from: "app/sw.ts"
      to: "app/~offline/page.tsx"
      via: "fallback URL for offline navigation"
      pattern: "~offline"
---

<objective>
Configure Serwist for PWA support with service worker, web manifest, offline fallback page, and deferred install prompt. The app becomes installable to mobile home screens with a dark/monochrome brutalist theme.

Purpose: Make the app feel native on mobile devices and provide graceful offline degradation from day one.
Output: Working PWA with install prompt, service worker caching, and branded offline page.
</objective>

<execution_context>
@C:/Users/somet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/somet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/acroyoga-academy/.planning/PROJECT.md
@C:/acroyoga-academy/.planning/ROADMAP.md
@C:/acroyoga-academy/.planning/phases/01-foundation-auth/01-RESEARCH.md
@C:/acroyoga-academy/.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@C:/acroyoga-academy/.planning/phases/01-foundation-auth/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Serwist service worker, web manifest, and Next.js config</name>
  <files>
    next.config.ts
    app/sw.ts
    app/manifest.ts
    public/icon-192x192.png
    public/icon-512x512.png
  </files>
  <action>
    Update `next.config.ts` to integrate Serwist:
    - Import `withSerwistInit` from "@serwist/next"
    - Import crypto for revision UUID
    - Configure withSerwist with:
      - swSrc: "app/sw.ts"
      - swDest: "public/sw.js"
      - additionalPrecacheEntries: [{ url: "/~offline", revision: crypto.randomUUID() }]
    - Wrap the existing Next.js config with withSerwist()
    - IMPORTANT per research: Do NOT use Turbopack. Remove any --turbopack flags from dev scripts in package.json if present.

    Create `app/sw.ts`:
    - Import defaultCache from "@serwist/next/worker"
    - Import Serwist from "serwist"
    - Declare `self.__SW_MANIFEST` for TypeScript
    - Create Serwist instance with:
      - precacheEntries: self.__SW_MANIFEST
      - skipWaiting: true
      - clientsClaim: true
      - navigationPreload: true
      - runtimeCaching: defaultCache
      - fallbacks entries: [{ url: "/~offline", matcher: ({ request }) => request.destination === "document" }]
    - Call serwist.addEventListeners()

    Create `app/manifest.ts`:
    - Export default function returning MetadataRoute.Manifest
    - Per user decision: "Dark/monochrome theme color from day one"
    - name: "AcroYoga Academy"
    - short_name: "AcroAcademy"
    - description: "Find acro partners and events near you"
    - start_url: "/"
    - display: "standalone"
    - background_color: "#0a0a0a" (dark brutalist)
    - theme_color: "#0a0a0a" (dark monochrome)
    - icons: 192x192 and 512x512 PNG references
    - Per user decision: "Placeholder app icon — will be replaced in Phase 2 (Brand Identity)"

    Create placeholder PWA icons:
    - Generate simple 192x192 and 512x512 PNG files in public/
    - These should be solid dark squares with "AA" text or a simple geometric mark in white
    - Use a canvas/sharp library to generate, OR create minimal valid PNG files
    - If image generation is complex, create solid-color placeholder PNGs using a Node script (write a small one-off script that uses the canvas API or just copy a minimal valid PNG buffer)

    Verify `.gitignore` includes `public/sw.js` and `public/sw.js.map` (should already be set from Plan 01).
  </action>
  <verify>
    Run `npm run build` — should complete without errors and generate `public/sw.js`. Run `npm run start` — visit `http://localhost:3000/manifest.json` and confirm it returns valid JSON with dark theme colors. Open DevTools > Application > Service Workers — confirm sw.js is registered. Check DevTools > Application > Manifest — confirm manifest loads with correct name, colors, and icons.
  </verify>
  <done>Serwist service worker builds and registers. Web manifest accessible with dark brutalist theme colors. Placeholder PWA icons in public/. sw.js generated during build.</done>
</task>

<task type="auto">
  <name>Task 2: Create offline fallback page, install prompt component, and iOS banner</name>
  <files>
    app/~offline/page.tsx
    src/components/pwa/InstallPrompt.tsx
    src/components/pwa/IosBanner.tsx
  </files>
  <action>
    Create `app/~offline/page.tsx`:
    - Per research recommendation: "Cached app shell with branded offline splash"
    - Static page (no dynamic data, no locale routing — sits outside [locale])
    - Dark background (neutral-950), centered content
    - Show app name "AcroYoga Academy" in Heebo/system font
    - Message: "You're offline — connect to continue" (English only — offline page is a fallback, not a full locale experience)
    - Simple, branded, matches the dark brutalist tone
    - Optional: Add a subtle geometric pattern or border for visual interest
    - No JavaScript required — pure HTML/CSS

    Create `src/components/pwa/InstallPrompt.tsx`:
    - "use client" component
    - Per user decision: "Install prompt shown after 2nd visit"
    - On mount: increment visit count in localStorage, listen for `beforeinstallprompt` event
    - Only show the install button if: visit count >= 2 AND beforeinstallprompt was captured AND user hasn't dismissed
    - Dismissal stored in localStorage ("pwa_install_dismissed")
    - UI: floating bottom banner with "Install App" button and dismiss X
    - Dark theme, translucent background, compact
    - On click Install: call deferredPrompt.prompt(), track outcome
    - On click dismiss: hide and set localStorage flag

    Create `src/components/pwa/IosBanner.tsx`:
    - "use client" component
    - Per research: "beforeinstallprompt does not fire on iOS Safari. Show a static banner with share icon instructions"
    - Detect iOS: /iPad|iPhone|iPod/.test(navigator.userAgent) && !window.MSStream
    - Also check: not already in standalone mode (window.matchMedia('(display-mode: standalone)').matches)
    - Show only if: iOS + not standalone + visit count >= 2 + not dismissed
    - UI: bottom banner with share icon (⬆) and text "Add to Home Screen: tap Share, then 'Add to Home Screen'"
    - Dismissable, remembers dismissal in localStorage
    - Same dark theme as InstallPrompt

    Add both InstallPrompt and IosBanner to the root locale layout (`app/[locale]/layout.tsx`) so they appear on all pages.
  </action>
  <verify>
    Run production build: `npm run build && npm run start`. Visit `http://localhost:3000` twice (or set localStorage visit_count=2 manually). The install prompt should appear on the 2nd visit (in Chrome/Edge — not Firefox which doesn't support beforeinstallprompt). Disconnect network (DevTools > Network > Offline) — navigate to a new page — offline fallback page should appear with "You're offline" message. Verify the offline page has the dark brutalist styling.
  </verify>
  <done>Offline fallback page shows branded "You're offline" message. Install prompt appears on 2nd visit with dismiss functionality. iOS banner shows share instructions for Safari users. All components integrated into the locale layout.</done>
</task>

</tasks>

<verification>
1. `npm run build` produces `public/sw.js` without errors
2. `/manifest.json` returns valid manifest with dark theme (#0a0a0a)
3. Service worker registers in browser DevTools
4. Offline fallback page appears when navigating with no network
5. Install prompt does NOT appear on first visit
6. Install prompt DOES appear on second visit (Chrome/Edge)
7. Dismissed install prompt does not reappear
8. PWA icons load (192x192 and 512x512)
9. App is installable (Chrome > Install App option available)
</verification>

<success_criteria>
- PWA manifest loads with dark/monochrome brutalist theme colors
- Service worker caches app shell and serves offline fallback
- Offline page shows branded "You're offline" message
- Install prompt deferred to 2nd visit per user decision
- iOS Safari gets share-button instructions banner
- Placeholder icons present (to be replaced in Phase 2)
- No Turbopack in build pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-04-SUMMARY.md`
</output>
