---
phase: 01-foundation-auth
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - src/components/layout/Header.tsx
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "User can sign up, log in, and stay logged in across browser sessions with email and password"
    - "User can reset a forgotten password via an emailed link"
    - "Visitor can toggle between Hebrew (RTL, Heebo font) and English on any page with no layout breaks"
    - "App can be installed to a mobile home screen and loads a skeleton UI offline"
    - "User must accept terms of service before completing signup; the acceptance is recorded"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Header with auth status, language toggle, sign-in/out"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "src/lib/auth-client.ts"
      via: "useSession for auth state display"
      pattern: "useSession"
---

<objective>
Final integration pass: wire auth status into the header (show signed-in user or sign-in link), verify the complete end-to-end flow across all four previous plans, and perform a human verification checkpoint on the full Phase 1 experience.

Purpose: Ensure all Phase 1 requirements work together as a cohesive application, not just individually.
Output: Integrated header with auth state, verified end-to-end flow, human confirmation.
</objective>

<execution_context>
@C:/Users/somet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/somet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/acroyoga-academy/.planning/PROJECT.md
@C:/acroyoga-academy/.planning/ROADMAP.md
@C:/acroyoga-academy/.planning/phases/01-foundation-auth/01-RESEARCH.md
@C:/acroyoga-academy/.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@C:/acroyoga-academy/.planning/phases/01-foundation-auth/01-02-SUMMARY.md
@C:/acroyoga-academy/.planning/phases/01-foundation-auth/01-03-SUMMARY.md
@C:/acroyoga-academy/.planning/phases/01-foundation-auth/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire auth status into Header and run automated integration checks</name>
  <files>
    src/components/layout/Header.tsx
  </files>
  <action>
    Update `src/components/layout/Header.tsx` to include auth-aware elements:
    - Use `useSession()` from auth-client to get current session state
    - When signed in: show user's name or email (truncated) and a "Sign Out" button on the trailing edge (before the language toggle)
    - When signed out: show "Sign In" link on the trailing edge
    - Sign Out button calls `authClient.signOut()` and redirects to home
    - Keep the language toggle always visible (per user decision: "always visible")
    - Layout order (LTR): App name ... [Sign In/User name + Sign Out] [flag toggle]
    - Layout order (RTL): [flag toggle] [Sign In/User name + Sign Out] ... App name
    - Use CSS logical properties for spacing between elements
    - Mobile: if space is tight, user name can be hidden (show just a user icon + sign out), flag toggle stays visible

    Run automated checks:
    1. `npx tsc --noEmit` — no TypeScript errors across the entire project
    2. `npm run build` — production build succeeds (includes Serwist sw.js generation)
    3. Verify all pages have setRequestLocale calls (grep for pattern)
    4. Verify middleware matcher excludes api, sw.js, manifest.json
    5. Verify no `--turbopack` in package.json scripts
  </action>
  <verify>
    Run `npm run build` — must complete successfully. Run `npx tsc --noEmit` — zero errors. Start prod server with `npm run start` and verify:
    - Visit `/en/` — header shows "Sign In" link and flag toggle
    - Sign in — header updates to show user name and "Sign Out"
    - Sign out — returns to header with "Sign In" link
    - Switch to Hebrew — header flips to RTL correctly
  </verify>
  <done>Header shows auth status (signed-in user or sign-in link). Production build passes. All TypeScript checks pass. Integration verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 1 end-to-end experience</name>
  <files>
    N/A — verification only, no files modified
  </files>
  <action>
    CHECKPOINT: Pause execution and present the following verification steps to the user.
    This is a human-verify checkpoint — Claude has completed all automated work. The user must now manually test the full Phase 1 experience end-to-end.

    What was built:
    - Bilingual Hebrew RTL / English routing with flag toggle
    - Email/password signup -> TOS acceptance -> onboarding wizard -> dashboard
    - Sign-in with session persistence (30 days)
    - Password reset via email
    - PWA installable with offline fallback
    - Dark/monochrome brutalist base theme

    Present the 6 test scenarios below and wait for user feedback.
  </action>
  <verify>
    User performs the following tests and reports results:

    **Test 1 — Language switching (FOUND-02):**
    1. Visit http://localhost:3000 — should redirect to /en/ or /he/ based on browser
    2. Click the Israeli flag — page switches to Hebrew, text is RTL, URL is /he/
    3. Click the US flag — page switches to English, text is LTR, URL is /en/
    4. Verify no layout breaks or overlap in either direction

    **Test 2 — Signup + TOS + Onboarding (AUTH-01, FOUND-05):**
    1. Click "Sign Up" — fill in name, email, password (8+ chars)
    2. After signup, land on a full-screen TOS page (not a checkbox)
    3. Read TOS — covers community conduct, privacy, data
    4. Click "I Accept" — land on the onboarding wizard
    5. Fill in city, select role (Base/Flyer/Both), select level
    6. After onboarding, reach the dashboard with "Welcome, [name]"

    **Test 3 — Session persistence (AUTH-02):**
    1. Close the browser completely
    2. Reopen and visit http://localhost:3000/en/dashboard
    3. Should still be logged in (not redirected to sign-in)

    **Test 4 — Password reset (AUTH-03):**
    1. Sign out, go to Sign In, click "Forgot password?"
    2. Enter email and submit
    3. If Resend configured: check email for reset link
    4. If Resend not configured: verify request doesn't crash, shows "Reset link sent"

    **Test 5 — PWA (FOUND-03):**
    1. DevTools > Application > Manifest — dark theme colors, app name, icons
    2. DevTools > Application > Service Workers — sw.js is active
    3. 2nd visit — install prompt appears (Chrome/Edge)
    4. Go offline (Network > Offline), navigate — offline page appears

    **Test 6 — RTL auth pages (FOUND-04):**
    1. Switch to Hebrew (/he/)
    2. Visit /he/sign-up and /he/sign-in — forms are RTL aligned
    3. Inputs, buttons, and links align correctly in RTL
  </verify>
  <done>User types "approved" confirming all 6 test scenarios pass, or describes issues to fix. All Phase 1 ROADMAP success criteria are met.</done>
</task>

</tasks>

<verification>
Phase 1 success criteria (from ROADMAP.md):
1. User can sign up, log in, and stay logged in across browser sessions with email and password
2. User can reset a forgotten password via an emailed link
3. Visitor can toggle between Hebrew (RTL, Heebo font) and English on any page with no layout breaks
4. App can be installed to a mobile home screen and loads a skeleton UI offline
5. User must accept terms of service before completing signup; the acceptance is recorded
</verification>

<success_criteria>
- All 5 ROADMAP success criteria verified by human
- Header shows auth state correctly in both locales
- Production build passes without errors
- No TypeScript errors in codebase
- Complete signup-to-dashboard flow works in both languages
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-05-SUMMARY.md`
</output>
