---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - src/i18n/navigation.ts
  - src/middleware.ts
  - app/[locale]/layout.tsx
  - app/[locale]/page.tsx
  - app/layout.tsx
  - messages/en.json
  - messages/he.json
  - src/components/layout/LanguageToggle.tsx
  - src/components/layout/Header.tsx
autonomous: true
requirements:
  - FOUND-02
  - FOUND-04

must_haves:
  truths:
    - "Visiting / redirects to /en/ or /he/ based on browser accept-language header"
    - "Visiting /he/ renders the page in Hebrew with dir=rtl on the html element"
    - "Visiting /en/ renders the page in English with dir=ltr on the html element"
    - "Flag toggle in header switches between /he/ and /en/ versions of the current page"
    - "Heebo font loads without layout shift on both Hebrew and English pages"
    - "Layout uses CSS logical properties and does not break in RTL mode"
  artifacts:
    - path: "src/i18n/routing.ts"
      provides: "next-intl routing config with en/he locales"
      exports: ["routing"]
    - path: "src/middleware.ts"
      provides: "next-intl middleware for locale detection and URL prefix routing"
      contains: "createMiddleware"
    - path: "app/[locale]/layout.tsx"
      provides: "Locale-aware root layout with dir, lang, Heebo font"
      contains: "dir.*locale"
    - path: "messages/en.json"
      provides: "English translation strings"
    - path: "messages/he.json"
      provides: "Hebrew translation strings"
    - path: "src/components/layout/LanguageToggle.tsx"
      provides: "Flag icon language toggle component"
      contains: "usePathname"
  key_links:
    - from: "src/middleware.ts"
      to: "src/i18n/routing.ts"
      via: "import routing config"
      pattern: "import.*routing"
    - from: "app/[locale]/layout.tsx"
      to: "messages/"
      via: "next-intl NextIntlClientProvider or getMessages"
      pattern: "getMessages|NextIntlClientProvider"
    - from: "src/components/layout/LanguageToggle.tsx"
      to: "src/i18n/navigation.ts"
      via: "typed Link or useRouter for locale switching"
      pattern: "useRouter|usePathname|Link"
---

<objective>
Set up next-intl URL-prefix bilingual routing (Hebrew RTL + English LTR), Heebo font loading, and a flag-icon language toggle. Every page will live inside `app/[locale]/` and inherit correct `dir`, `lang`, and font settings. The middleware handles browser locale detection and redirects.

Purpose: Establish the i18n layer that all UI pages (auth, dashboard, etc.) build on. Getting RTL right from the start prevents expensive retrofitting.
Output: Working bilingual routing with Hebrew RTL support, language toggle, and all base translation strings.
</objective>

<execution_context>
@C:/Users/somet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/somet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/acroyoga-academy/.planning/PROJECT.md
@C:/acroyoga-academy/.planning/ROADMAP.md
@C:/acroyoga-academy/.planning/phases/01-foundation-auth/01-RESEARCH.md
@C:/acroyoga-academy/.planning/phases/01-foundation-auth/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure next-intl routing, middleware, and locale-aware layout with Heebo font</name>
  <files>
    src/i18n/routing.ts
    src/i18n/request.ts
    src/i18n/navigation.ts
    src/middleware.ts
    app/layout.tsx
    app/[locale]/layout.tsx
    app/[locale]/page.tsx
  </files>
  <action>
    Create `src/i18n/routing.ts`:
    - defineRouting with locales: ["en", "he"], defaultLocale: "en", localeDetection: true

    Create `src/i18n/request.ts`:
    - Import getRequestConfig from "next-intl/server" and routing
    - Export default getRequestConfig that loads messages from `messages/${locale}.json`
    - Validate locale against routing.locales

    Create `src/i18n/navigation.ts`:
    - Import createNavigation from "next-intl/navigation" and routing
    - Export typed { Link, redirect, usePathname, useRouter } from createNavigation(routing)

    Create `src/middleware.ts`:
    - Import createMiddleware from "next-intl/middleware" and routing
    - Export default createMiddleware(routing)
    - CRITICAL: Set matcher to exclude API routes, Next.js internals, AND PWA files:
      ```
      export const config = {
        matcher: "/((?!api|trpc|_next|_vercel|sw\\.js|manifest\\.json|icon.*\\.png|.*\\..*).*)",
      };
      ```
    - Do NOT add Better Auth to middleware — auth checks happen in Server Components only (CVE-2025-29927 protection per research)

    Update `app/layout.tsx`:
    - Minimal root layout — just html/body wrapper. Do NOT set lang/dir here (that's in [locale]/layout.tsx)
    - Import global CSS

    Create `app/[locale]/layout.tsx`:
    - Import Heebo from "next/font/google" with subsets ["latin", "hebrew"], weights ["400", "500", "700", "900"], display: "swap", variable: "--font-heebo", preload: true
    - Determine dir from locale (he = rtl, en = ltr)
    - Set `<html lang={locale} dir={dir} className={heebo.variable}>` 
    - Use NextIntlClientProvider with messages from getMessages()
    - Call setRequestLocale(locale) for static rendering support
    - Add generateStaticParams returning [{ locale: "en" }, { locale: "he" }]
    - Apply dark background (bg-neutral-950 text-neutral-100) for brutalist tone per user decision
    - Include the Header component (created in Task 2)

    Create `app/[locale]/page.tsx`:
    - Simple home page using useTranslations("home") with a welcome heading and brief description
    - Call setRequestLocale(locale)
    - Mobile-first layout: single column, centered content, generous padding
    - Use CSS logical properties (ps-4, pe-4, ms-auto, me-auto) instead of physical (pl, pr, ml, mr) for RTL compatibility per FOUND-04

    Update Tailwind config (or `app/globals.css` if Tailwind v4 uses CSS config):
    - Set font-sans to use var(--font-heebo) with fallbacks
    - Ensure dark theme colors are set (neutral-950 background, neutral-100 text)
  </action>
  <verify>
    Run `npm run dev`. Visit `http://localhost:3000` — should redirect to `/en/` or `/he/` based on browser locale. Visit `/he/` — page should render RTL with Hebrew text. Visit `/en/` — page should render LTR with English text. Check that Heebo font loads (inspect Network tab for font file or check computed font-family). Verify `dir="rtl"` on `<html>` when visiting `/he/`.
  </verify>
  <done>next-intl locale detection and URL-prefix routing works. Hebrew pages render RTL, English pages render LTR. Heebo font loads without layout shift. Middleware excludes API and PWA paths.</done>
</task>

<task type="auto">
  <name>Task 2: Create translation files and flag-icon language toggle component</name>
  <files>
    messages/en.json
    messages/he.json
    src/components/layout/LanguageToggle.tsx
    src/components/layout/Header.tsx
  </files>
  <action>
    Create `messages/en.json` with initial namespace structure:
    ```json
    {
      "common": {
        "appName": "AcroYoga Academy",
        "switchLanguage": "Switch language"
      },
      "home": {
        "title": "Welcome to AcroYoga Academy",
        "subtitle": "Find your perfect acro partner"
      },
      "auth": {
        "signIn": "Sign In",
        "signUp": "Sign Up",
        "email": "Email",
        "password": "Password",
        "name": "Full Name",
        "forgotPassword": "Forgot password?",
        "resetPassword": "Reset Password",
        "sendResetLink": "Send Reset Link",
        "noAccount": "Don't have an account?",
        "hasAccount": "Already have an account?",
        "signOut": "Sign Out"
      },
      "tos": {
        "title": "Terms of Service & Privacy Policy",
        "accept": "I Accept",
        "decline": "Decline",
        "mustAccept": "You must accept the terms to use the platform"
      },
      "onboarding": {
        "title": "Set Up Your Profile",
        "city": "City",
        "cityPlaceholder": "e.g., Tel Aviv",
        "role": "Preferred Role",
        "roleBase": "Base",
        "roleFlyer": "Flyer",
        "roleBoth": "Both",
        "level": "Experience Level",
        "levelBeginner": "Beginner",
        "levelIntermediate": "Intermediate",
        "levelAdvanced": "Advanced",
        "continue": "Continue",
        "skip": "Skip for now"
      },
      "offline": {
        "title": "You're Offline",
        "message": "Connect to the internet to continue"
      }
    }
    ```

    Create `messages/he.json` with the same structure, all values in Hebrew:
    - appName: "אקדמיית אקרויוגה"
    - home.title: "ברוכים הבאים לאקדמיית אקרויוגה"
    - home.subtitle: "מצאו את הפרטנר המושלם לאקרו"
    - auth.signIn: "התחברות", signUp: "הרשמה", email: "אימייל", password: "סיסמה", name: "שם מלא", forgotPassword: "שכחת סיסמה?", resetPassword: "איפוס סיסמה", sendResetLink: "שלח קישור לאיפוס", noAccount: "אין לך חשבון?", hasAccount: "כבר יש לך חשבון?", signOut: "התנתקות"
    - tos.title: "תנאי שימוש ומדיניות פרטיות", accept: "אני מסכים/ה", decline: "לא מסכים/ה", mustAccept: "יש לאשר את תנאי השימוש כדי להשתמש בפלטפורמה"
    - onboarding.title: "הגדרת הפרופיל שלך", city: "עיר", cityPlaceholder: "לדוגמה: תל אביב", role: "תפקיד מועדף", roleBase: "בייס", roleFlyer: "פלייר", roleBoth: "שניהם", level: "רמת ניסיון", levelBeginner: "מתחיל/ה", levelIntermediate: "בינוני/ת", levelAdvanced: "מתקדם/ת", continue: "המשך", skip: "דלג לעת עתה"
    - offline.title: "אין חיבור לאינטרנט", message: "התחבר לאינטרנט כדי להמשיך"

    Create `src/components/layout/LanguageToggle.tsx`:
    - "use client" component
    - Import usePathname, useRouter from "@/i18n/navigation" and useLocale from "next-intl"
    - Display two flag buttons: Israeli flag (🇮🇱) for Hebrew, US flag (🇺🇸) for English
    - Per user decision: "Flag icons (IL/US) in the header — visual, compact, always visible"
    - Active locale flag should be visually distinct (e.g., ring highlight, opacity change)
    - On click, use router.replace(pathname, { locale: targetLocale }) to switch
    - Compact layout — just the two flag buttons side by side, no text labels
    - aria-label for accessibility using useTranslations("common").switchLanguage

    Create `src/components/layout/Header.tsx`:
    - Simple header bar: app name on the start side (leading edge, adapts to RTL), language toggle on the end side (trailing edge)
    - Use CSS logical properties for positioning (ms-auto, me-auto, ps-4, pe-4)
    - Dark theme consistent with brutalist tone (neutral-900 background, border-neutral-800 bottom border)
    - Mobile-first: full width, fixed height, horizontally padded
    - Will be extended in Plan 03 to include auth status (sign in/out buttons)
  </action>
  <verify>
    Run `npm run dev`. Visit `/en/` — page shows English text, English flag highlighted. Click Israeli flag — URL changes to `/he/`, page renders in Hebrew with RTL direction, Hebrew flag highlighted. Click US flag — returns to `/en/` with LTR. Verify no layout shifts or broken elements when switching between RTL and LTR. Check that all translation keys render (no missing key warnings in console).
  </verify>
  <done>English and Hebrew translation files complete with all Phase 1 namespaces. Flag-icon language toggle switches locale via URL. Header renders correctly in both RTL and LTR. No missing translation warnings.</done>
</task>

</tasks>

<verification>
1. `http://localhost:3000` redirects to `/en/` or `/he/` based on browser locale
2. `/he/` page has `<html lang="he" dir="rtl">`
3. `/en/` page has `<html lang="en" dir="ltr">`
4. Flag toggle switches between locales without page reload flash
5. Heebo font is loaded and applied (visible in Hebrew characters)
6. No console warnings about missing translation keys
7. Layout does not break in RTL mode (no horizontal overflow, correct alignment)
8. Middleware does not intercept `/api/*`, `/sw.js`, or `/manifest.json` paths
</verification>

<success_criteria>
- URL-prefix bilingual routing works (/en/ and /he/)
- Browser locale detection redirects correctly
- RTL/LTR direction set correctly on html element per locale
- Heebo font loads with swap display strategy (no layout shift)
- Flag-icon language toggle visible in header, switches locale
- All Phase 1 translation strings present in both languages
- CSS logical properties used throughout for RTL compatibility
- Mobile-first responsive layout established
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>
