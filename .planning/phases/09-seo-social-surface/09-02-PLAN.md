---
phase: 09-seo-social-surface
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/lib/db/schema.ts
  - src/app/[locale]/(public)/layout.tsx
  - src/app/[locale]/(public)/cities/[city]/page.tsx
  - src/components/city/CityHero.tsx
  - src/components/city/CityJamList.tsx
  - src/components/city/CityStats.tsx
  - src/components/city/CityFAQ.tsx
  - src/proxy.ts
  - messages/en.json
  - messages/he.json
autonomous: true
requirements:
  - SEO-02

must_haves:
  truths:
    - "City landing pages exist for Tel Aviv and Kfar Saba at /he/cities/tel-aviv and /he/cities/kfar-saba"
    - "City pages are fully public -- no login required, accessible to search bots"
    - "City pages have LocalBusiness + FAQPage JSON-LD structured data in the HTML"
    - "City pages show upcoming jams in that city, community stats, FAQ section, and relevant CTA"
    - "City pages have correct Hebrew meta tags targeting local acroyoga search terms"
  artifacts:
    - path: "src/app/[locale]/(public)/cities/[city]/page.tsx"
      provides: "City landing page with generateMetadata and generateStaticParams"
      contains: "generateStaticParams"
    - path: "src/app/[locale]/(public)/layout.tsx"
      provides: "Public route group layout without auth guard"
    - path: "src/components/city/CityHero.tsx"
      provides: "City hero section with name and CTA"
    - path: "src/components/city/CityJamList.tsx"
      provides: "Upcoming jams filtered by city"
    - path: "src/components/city/CityStats.tsx"
      provides: "Community member count and jam stats per city"
    - path: "src/components/city/CityFAQ.tsx"
      provides: "FAQ section with AEO-optimized Hebrew Q&A"
  key_links:
    - from: "src/app/[locale]/(public)/cities/[city]/page.tsx"
      to: "src/lib/seo/metadata.ts"
      via: "generateMetadata uses buildPageMetadata for city-specific SEO"
      pattern: "buildPageMetadata"
    - from: "src/app/[locale]/(public)/cities/[city]/page.tsx"
      to: "src/lib/seo/schemas.ts"
      via: "LocalBusiness + FAQPage JSON-LD schemas injected via JsonLd component"
      pattern: "buildLocalBusinessSchema|buildFAQSchema"
    - from: "src/components/city/CityJamList.tsx"
      to: "src/lib/db"
      via: "Drizzle query filtering jams by city column"
      pattern: "jamSessions.*city"
---

<objective>
City landing pages for Tel Aviv and Kfar Saba with structured data, FAQ, and community stats

Purpose: Create SEO-optimized public landing pages for the two target cities so the platform ranks for Hebrew acroyoga search terms. City pages are the primary organic acquisition channel.
Output: Public route group with Tel Aviv and Kfar Saba city pages, each containing hero + CTA, upcoming jams, community stats, AEO-optimized FAQ with FAQPage schema, LocalBusiness JSON-LD, and Hebrew meta tags.
</objective>

<execution_context>
@C:/Users/somet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/somet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/acroyoga-academy/.planning/PROJECT.md
@C:/acroyoga-academy/.planning/ROADMAP.md
@C:/acroyoga-academy/.planning/phases/09-seo-social-surface/09-CONTEXT.md
@C:/acroyoga-academy/.planning/phases/09-seo-social-surface/09-RESEARCH.md
@C:/acroyoga-academy/.planning/phases/09-seo-social-surface/09-01-SUMMARY.md
@C:/acroyoga-academy/src/lib/db/schema.ts
@C:/acroyoga-academy/src/proxy.ts
@C:/acroyoga-academy/messages/en.json
@C:/acroyoga-academy/messages/he.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration (add city to jamSessions) + public route group + city page with metadata and JSON-LD</name>
  <files>
    src/lib/db/schema.ts
    src/app/[locale]/(public)/layout.tsx
    src/app/[locale]/(public)/cities/[city]/page.tsx
    src/proxy.ts
    messages/en.json
    messages/he.json
  </files>
  <action>
1. Add `city` column to `jamSessions` table in `src/lib/db/schema.ts`:
   - `city: text("city")` -- nullable (existing jams may not have city set)
   - This enables filtering jams by city for city landing pages
   - Run `npx drizzle-kit push` to apply the migration to Neon

2. Create `src/app/[locale]/(public)/layout.tsx`:
   - Simple pass-through layout with NO auth guard
   - Just returns `{children}` wrapped in any necessary container
   - This route group is for public pages that must be crawlable by bots

3. Update `src/proxy.ts` (or middleware):
   - Ensure `/cities/*` routes are NOT subject to auth checks
   - The `(public)` route group should naturally bypass auth since it is a separate route group from `(app)`, but verify no global middleware catches it

4. Create `src/app/[locale]/(public)/cities/[city]/page.tsx`:
   - `generateStaticParams` returns 4 combos: `{ locale: 'he', city: 'tel-aviv' }`, `{ locale: 'he', city: 'kfar-saba' }`, `{ locale: 'en', city: 'tel-aviv' }`, `{ locale: 'en', city: 'kfar-saba' }`
   - `revalidate = 3600` (hourly ISR for fresh jam data)
   - `generateMetadata` calls `buildPageMetadata({ locale, namespace: 'seo.city.[city slug]', path: '/cities/[city]' })` -- use the city-specific i18n namespace (e.g., `seo.cityTelAviv`)
   - Page body renders:
     a. `<JsonLd data={buildLocalBusinessSchema(city)} />`
     b. `<JsonLd data={buildFAQSchema(faqItems)} />` (FAQ items from city-specific data)
     c. `<CityHero />` -- hero section
     d. `<CityJamList />` -- upcoming jams RSC
     e. `<CityStats />` -- member/jam count
     f. `<CityFAQ />` -- FAQ section
   - If city param is not 'tel-aviv' or 'kfar-saba', call `notFound()`

5. Add i18n keys for city pages:
   - `city.telAviv.heroTitle`, `city.telAviv.heroCta` -- Hebrew: "אקרויוגה בתל אביב", CTA to join
   - `city.kfarSaba.heroTitle`, `city.kfarSaba.heroCta` -- Hebrew: "אקרויוגה בכפר סבא"
   - `city.noJams` -- "אין ג'אמים קרובים" / "No upcoming jams"
   - `city.stats.members`, `city.stats.jams`, `city.stats.title`
   - `city.faq.title` -- "שאלות נפוצות" / "FAQ"
  </action>
  <verify>
1. `npx drizzle-kit push` succeeds (city column added to jam_sessions)
2. `npx tsc --noEmit` passes
3. `npm run build` succeeds with the (public) route group and static params
  </verify>
  <done>
jam_sessions table has city column. Public route group exists with no auth guard. City pages pre-render for 4 locale/city combos. generateMetadata produces city-specific Hebrew SEO meta. LocalBusiness and FAQPage JSON-LD schemas are injected.
  </done>
</task>

<task type="auto">
  <name>Task 2: City page components (Hero, JamList, Stats, FAQ) with AEO-optimized Hebrew content</name>
  <files>
    src/components/city/CityHero.tsx
    src/components/city/CityJamList.tsx
    src/components/city/CityStats.tsx
    src/components/city/CityFAQ.tsx
  </files>
  <action>
1. Create `src/components/city/CityHero.tsx` (Server Component):
   - Accepts `{ city, locale }` props
   - Displays city name in Hebrew prominently (h1)
   - Subtitle with local acroyoga community description using Hebrew keywords (cover both "אקרויוגה" and "אקרו יוגה" spellings in the copy)
   - CTA button linking to sign-up or challenge quiz (primary conversion path)
   - Dark background, brand pink accent, brutalist bold typography
   - Use `getTranslations` for bilingual text

2. Create `src/components/city/CityJamList.tsx` (Server Component):
   - Accepts `{ city, locale }` props
   - Queries `jamSessions` WHERE `city = city` AND `scheduledAt >= now()` ORDER BY `scheduledAt ASC` LIMIT 6
   - Falls back to text-matching `location` field with ILIKE against city name if `city` column is null (for existing jams without city set)
   - Renders each jam as a card: date, location, level, capacity/spots remaining
   - Each card links to `/jams` page (or jam detail if it exists)
   - If no jams found, show encouraging empty state: "No upcoming jams in [city] yet -- be the first to host!"
   - Include `<JsonLd data={buildEventSchema(jam)} />` for each jam (Event structured data)

3. Create `src/components/city/CityStats.tsx` (Server Component):
   - Accepts `{ city }` props
   - Queries: count of users WHERE `city` matches (for member count), count of future jams in city
   - Displays 2-3 stat cards: "X members", "Y upcoming jams"
   - Brutalist card style with large numbers, brand pink highlights

4. Create `src/components/city/CityFAQ.tsx` (Server Component):
   - Accepts `{ city, locale }` props
   - Uses the FAQ content from 09-RESEARCH.md Hebrew Acroyoga Keyword section verbatim:
     - "What is acroyoga?" / "Who can do it?" / "Base vs flyer?" / "How much does a jam cost?"
     - City-specific: "Where do jams happen in [Tel Aviv/Kfar Saba]?"
     - "Is acroyoga dangerous?"
   - FAQ items are bilingual objects (inline `{ he: string, en: string }`) -- NOT from i18n files (same pattern as quiz FAQ from Phase 05-04)
   - Renders as collapsible accordion using Radix Accordion or simple disclosure elements
   - Each Q/A is an h3 + paragraph for semantic HTML (good for AEO crawlers)
   - AEO optimization: answers are direct, conversational, under 3 sentences each (per research guidance)
  </action>
  <verify>
1. `npx tsc --noEmit` passes
2. `npm run dev` then visit `http://localhost:3000/he/cities/tel-aviv` -- city page renders with hero, jam list (possibly empty), stats, and FAQ
3. View page source -- LocalBusiness JSON-LD and FAQPage JSON-LD both present in HTML
4. Visit `http://localhost:3000/he/cities/kfar-saba` -- same structure, Kfar Saba content
  </verify>
  <done>
Tel Aviv city page shows hero with Hebrew acroyoga keywords, upcoming jams (or empty state), community stats, and AEO-optimized FAQ. Kfar Saba city page shows equivalent content. Both have LocalBusiness + FAQPage + Event JSON-LD. FAQ answers are direct Hebrew responses targeting AI search engine citation.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` and `npm run build` pass
- `/he/cities/tel-aviv` renders full city page (hero, jams, stats, FAQ) without requiring login
- `/he/cities/kfar-saba` renders full city page without requiring login
- View source of city pages shows: LocalBusiness JSON-LD, FAQPage JSON-LD, Event JSON-LD (if jams exist), city-specific Hebrew meta tags, hreflang alternates
- City pages are listed in `/sitemap.xml` (from Plan 01)
- FAQ content directly answers common Hebrew acroyoga questions in conversational style
</verification>

<success_criteria>
SEO-02 is met: city landing pages exist for Tel Aviv and Kfar Saba targeting local Hebrew acroyoga search terms, with structured data (LocalBusiness, FAQPage, Event), and are fully public for search engine indexing. AEO-optimized FAQ sections target AI search engine citation.
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo-social-surface/09-02-SUMMARY.md`
</output>
