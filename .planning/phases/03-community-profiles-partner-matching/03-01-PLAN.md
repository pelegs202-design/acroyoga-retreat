---
phase: 03-community-profiles-partner-matching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/skills-data.ts
  - messages/en.json
  - messages/he.json
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-06

must_haves:
  truths:
    - "User table has bio and skills columns available for profile data"
    - "Reviews table exists with thumbs_up, comment, reviewer/reviewee references"
    - "Static skills data defines ~60 moves across 7 categories"
    - "All Phase 3 i18n keys exist in both en.json and he.json"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Extended user table with bio + skills columns; reviews table with indexes"
      contains: "bio.*text|skills.*array|reviews.*pgTable"
    - path: "src/lib/skills-data.ts"
      provides: "Static acroyoga skills constant with categories and moves"
      exports: ["SKILL_CATEGORIES", "ALL_MOVES"]
    - path: "messages/en.json"
      provides: "English i18n keys for profile, members, review namespaces"
      contains: "profile.*editButton|members.*title|review.*submit"
    - path: "messages/he.json"
      provides: "Hebrew i18n keys for profile, members, review namespaces"
      contains: "profile|members|review"
  key_links:
    - from: "src/lib/db/schema.ts"
      to: "reviews table"
      via: "foreign key references"
      pattern: "references.*user\\.id"
    - from: "src/lib/skills-data.ts"
      to: "user.skills column"
      via: "text array stores skill IDs from SKILL_CATEGORIES"
      pattern: "SKILL_CATEGORIES"
---

<objective>
Extend the database schema with profile fields (bio, skills) and reviews table, create the static acroyoga skills data, and add all Phase 3 i18n translation keys.

Purpose: Every other Phase 3 plan depends on these columns, the reviews table, the skills constant, and the translation keys existing first.
Output: Updated schema.ts, new skills-data.ts, updated en.json + he.json, applied Drizzle migration.
</objective>

<execution_context>
@C:/Users/somet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/somet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-community-profiles-partner-matching/03-CONTEXT.md
@.planning/phases/03-community-profiles-partner-matching/03-RESEARCH.md
@src/lib/db/schema.ts
@messages/en.json
@messages/he.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend user schema + create reviews table + generate migration</name>
  <files>src/lib/db/schema.ts</files>
  <action>
Add two new columns to the `user` pgTable definition:
- `bio: text("bio")` — nullable, for user bio text
- `skills: text("skills").array().notNull().default(sql`'{}'::text[]'`)` — text array for selected skill IDs, defaults to empty array. Import `sql` from `drizzle-orm` (already imported).

Add the `reviews` pgTable below the `tosAcceptances` table:
```
export const reviews = pgTable("reviews", {
  id: text("id").primaryKey(),
  reviewerId: text("reviewer_id").notNull().references(() => user.id, { onDelete: 'cascade' }),
  revieweeId: text("reviewee_id").notNull().references(() => user.id, { onDelete: 'cascade' }),
  jamSessionId: text("jam_session_id"), // nullable — stub until Phase 4 jam board exists
  thumbsUp: boolean("thumbs_up").notNull(),
  comment: text("comment"),
  createdAt: timestamp("created_at").defaultNow().notNull(),
}, (table) => [
  index("reviews_reviewee_idx").on(table.revieweeId),
  index("reviews_reviewer_idx").on(table.reviewerId),
]);
```

Add relations for reviews:
```
export const reviewsRelations = relations(reviews, ({ one }) => ({
  reviewer: one(user, { fields: [reviews.reviewerId], references: [user.id], relationName: 'reviewsGiven' }),
  reviewee: one(user, { fields: [reviews.revieweeId], references: [user.id], relationName: 'reviewsReceived' }),
}));
```

Update `userRelations` to include reviews:
```
export const userRelations = relations(user, ({ many }) => ({
  sessions: many(session),
  accounts: many(account),
  reviewsGiven: many(reviews, { relationName: 'reviewsGiven' }),
  reviewsReceived: many(reviews, { relationName: 'reviewsReceived' }),
}));
```

Then run migration:
```bash
npx drizzle-kit generate
npx drizzle-kit migrate
```

If migration fails because DB is not accessible in dev, that's fine — generate the SQL files and note it. The migration will be applied on deploy or when DB is available.
  </action>
  <verify>
Run `npx drizzle-kit generate` — should produce a migration SQL file in `./drizzle/` with ALTER TABLE for bio, skills columns and CREATE TABLE for reviews.
Verify schema.ts compiles: `npx tsc --noEmit src/lib/db/schema.ts` or `npm run build` (build should not fail on schema types).
  </verify>
  <done>
schema.ts has bio + skills columns on user table, reviews table with foreign keys and indexes, relations updated, and a Drizzle migration file generated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create static acroyoga skills data</name>
  <files>src/lib/skills-data.ts</files>
  <action>
Create `src/lib/skills-data.ts` with the following content. This is a static constant — NOT stored in the database. User's selected skills (stored in `user.skills` text[]) reference move names from this list.

```typescript
export type SkillCategory = {
  id: string;
  label: string;
  moves: string[];
};

export const SKILL_CATEGORIES: SkillCategory[] = [
  {
    id: "warm_ups",
    label: "Warm-Ups & Foundations",
    moves: [
      "Front Plank", "Back Plank", "Throne", "Half Frog",
      "Whale", "Toad", "Folded Leaf", "High Flying Whale",
    ],
  },
  {
    id: "l_basing_basics",
    label: "L-Basing Basics",
    moves: [
      "Bird", "Star", "Side Star", "Folded Bird",
      "Reverse Bird", "Foot to Hand (Low)", "Foot to Hand (High)",
      "Shoulderstand", "Candlestick",
    ],
  },
  {
    id: "l_basing_intermediate",
    label: "L-Basing Intermediate",
    moves: [
      "Bow", "Ninja Star", "Back Bird", "Free Star",
      "Hand to Hand (Low)", "Hand to Hand (Extended)", "Barrel Roll",
      "Bed / Log", "Ballerina",
    ],
  },
  {
    id: "washing_machines",
    label: "Washing Machines",
    moves: [
      "Helicopter", "Boomerang", "Corkscrew", "Catherine's Wheel",
      "Nunchucks", "Monkey Frog", "Star Wars", "Swimming Mermaid",
    ],
  },
  {
    id: "inversions",
    label: "Inversions",
    moves: [
      "Supported Handstand", "Foot to Handstand", "Straddle Bat",
      "Tuck Bat", "Cartwheel Entrance", "Icarian Press",
      "Straight Body Press to Handstand",
    ],
  },
  {
    id: "standing",
    label: "Standing Acrobatics",
    moves: [
      "Shoulder Stand (Standing)", "Vertical Dance",
      "Front Bird (Standing)", "Hand to Hand (Standing)",
      "Back Sit", "Shoulder Sit",
    ],
  },
  {
    id: "flows",
    label: "Flows & Sequences",
    moves: [
      "Bird to Star", "Star to Star", "Star to Throne",
      "Reverse Throne Combo", "F2S to S2F",
      "Ninja Flow", "Washing Machine into Inversion",
    ],
  },
];

export const ALL_MOVES = SKILL_CATEGORIES.flatMap(c => c.moves);
```

Total: ~60 moves across 7 categories. Sourced from Partner Acrobatics Manual taxonomy.
  </action>
  <verify>
Import compiles: add a temp `import { ALL_MOVES } from '@/lib/skills-data'` in any existing file and run `npx tsc --noEmit`. Or just verify the file has no syntax errors via direct TS check.
Verify `ALL_MOVES.length` is approximately 54-60.
  </verify>
  <done>
skills-data.ts exports SKILL_CATEGORIES (7 categories) and ALL_MOVES (~60 move strings). File compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 3 i18n translation keys (en + he)</name>
  <files>messages/en.json, messages/he.json</files>
  <action>
Add three new top-level namespaces to `messages/en.json`:

```json
"profile": {
  "editButton": "Edit Profile",
  "bio": "About",
  "bioPlaceholder": "Tell partners about your acro journey...",
  "bioEmpty": "Add your bio to help partners find you",
  "skills": "Skills",
  "skillsEmpty": "Select your known moves",
  "reviews": "Feedback",
  "reviewsEmpty": "No feedback yet",
  "reviewCount": "{count} reviews received",
  "photoUpload": "Change Photo",
  "andMore": "and {count} more",
  "saveSuccess": "Profile updated",
  "saveError": "Failed to update profile",
  "photoSuccess": "Photo updated",
  "photoError": "Failed to upload photo",
  "city": "City",
  "role": "Role",
  "level": "Level"
},
"members": {
  "title": "Find a Partner",
  "filterCity": "City",
  "filterRole": "Role",
  "filterLevel": "Level",
  "filterAll": "All",
  "noResults": "No partners found",
  "noResultsHint": "Try removing the {filter} filter",
  "searchPlaceholder": "Search by city...",
  "viewProfile": "View Profile"
},
"review": {
  "thumbsUp": "Great partner",
  "thumbsDown": "Needs improvement",
  "commentPlaceholder": "Optional note (1-2 sentences)...",
  "submit": "Leave Feedback",
  "success": "Feedback submitted",
  "alreadyReviewed": "You've already reviewed this partner recently"
}
```

Add matching Hebrew translations to `messages/he.json`:

```json
"profile": {
  "editButton": "עריכת פרופיל",
  "bio": "אודות",
  "bioPlaceholder": "ספרו לשותפים על המסע שלכם באקרויוגה...",
  "bioEmpty": "הוסיפו ביו כדי לעזור לשותפים למצוא אתכם",
  "skills": "מיומנויות",
  "skillsEmpty": "בחרו את התנועות שאתם יודעים",
  "reviews": "משוב",
  "reviewsEmpty": "אין משוב עדיין",
  "reviewCount": "{count} משובים התקבלו",
  "photoUpload": "שינוי תמונה",
  "andMore": "ועוד {count}",
  "saveSuccess": "הפרופיל עודכן",
  "saveError": "עדכון הפרופיל נכשל",
  "photoSuccess": "התמונה עודכנה",
  "photoError": "העלאת התמונה נכשלה",
  "city": "עיר",
  "role": "תפקיד",
  "level": "רמה"
},
"members": {
  "title": "מצאו שותף",
  "filterCity": "עיר",
  "filterRole": "תפקיד",
  "filterLevel": "רמה",
  "filterAll": "הכל",
  "noResults": "לא נמצאו שותפים",
  "noResultsHint": "נסו להסיר את הסינון לפי {filter}",
  "searchPlaceholder": "חיפוש לפי עיר...",
  "viewProfile": "צפייה בפרופיל"
},
"review": {
  "thumbsUp": "שותף מעולה",
  "thumbsDown": "צריך שיפור",
  "commentPlaceholder": "הערה אופציונלית (משפט-שניים)...",
  "submit": "שליחת משוב",
  "success": "המשוב נשלח",
  "alreadyReviewed": "כבר נתתם משוב לשותף הזה לאחרונה"
}
```

Preserve all existing keys. Add new namespaces after the last existing top-level key (after "offline").
  </action>
  <verify>
Both JSON files parse without errors: `node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8'))"` and same for he.json.
Verify keys exist: `node -e "const j=JSON.parse(require('fs').readFileSync('messages/en.json','utf8')); console.log(Object.keys(j.profile).length, Object.keys(j.members).length, Object.keys(j.review).length)"` — should output 3 numbers > 0.
  </verify>
  <done>
en.json has profile (17 keys), members (9 keys), review (6 keys) namespaces. he.json has matching Hebrew translations for all keys.
  </done>
</task>

</tasks>

<verification>
1. `npx drizzle-kit generate` produces migration SQL with bio, skills columns and reviews table
2. `npm run build` completes without TypeScript errors
3. Both en.json and he.json parse as valid JSON with profile, members, review namespaces
4. skills-data.ts exports SKILL_CATEGORIES (7 categories) and ALL_MOVES (~60 items)
</verification>

<success_criteria>
- User table schema includes bio (text, nullable) and skills (text[], default empty)
- Reviews table exists with reviewer/reviewee foreign keys, thumbs_up boolean, optional comment, indexes
- Static skills data defines 7 categories with ~60 total moves
- All Phase 3 translation keys present in both en.json and he.json
- Drizzle migration file generated
</success_criteria>

<output>
After completion, create `.planning/phases/03-community-profiles-partner-matching/03-01-SUMMARY.md`
</output>
