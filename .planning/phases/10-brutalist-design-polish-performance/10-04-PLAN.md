---
phase: 10-brutalist-design-polish-performance
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03"]
files_modified:
  - src/app/[locale]/(public)/cities/[city]/page.tsx
  - src/components/city/CityHero.tsx
  - src/components/city/CityFAQ.tsx
  - src/app/[locale]/(app)/quiz/challenge/page.tsx
  - src/components/quiz/QuizResultsPage.tsx
  - src/components/quiz/QuizCard.tsx
autonomous: true
requirements: [DSGN-01, DSGN-04, DSGN-05]

must_haves:
  truths:
    - "City landing pages have brutalist design matching the homepage aesthetic"
    - "City pages have scroll-reveal sections and card hover effects"
    - "Quiz challenge flow has brutalist card styling with dramatic hover"
    - "Quiz results page has scroll-reveal sections and bold brutalist typography"
    - "City and quiz pages are public-facing and feel cohesively brutalist with the homepage"
  artifacts:
    - path: "src/components/city/CityHero.tsx"
      provides: "Brutalist city hero section with bold typography"
      min_lines: 30
    - path: "src/app/[locale]/(public)/cities/[city]/page.tsx"
      provides: "City page composing brutalist sections"
      min_lines: 80
    - path: "src/components/quiz/QuizResultsPage.tsx"
      provides: "Brutalist-polished quiz results page"
      min_lines: 50
  key_links:
    - from: "src/app/[locale]/(public)/cities/[city]/page.tsx"
      to: "src/components/effects/ScrollReveal.tsx"
      via: "wrap city page sections"
      pattern: "import.*ScrollReveal"
    - from: "src/components/quiz/QuizResultsPage.tsx"
      to: "src/components/effects/ScrollReveal.tsx"
      via: "wrap results sections"
      pattern: "import.*ScrollReveal"
    - from: "src/components/quiz/QuizCard.tsx"
      to: "card-hover class"
      via: "class applied to option cards"
      pattern: "card-hover"
---

<objective>
Apply brutalist design polish to city landing pages (Tel Aviv + Kfar Saba) and quiz flow pages (challenge quiz + results page) to create a cohesive brutalist experience across all public-facing pages.

Purpose: City pages and quiz pages are conversion-critical public surfaces. They must match the homepage's brutalist aesthetic to maintain brand coherence across the user journey: homepage -> city page or quiz -> conversion.

Output: Polished city pages with scroll-reveal sections and brutalist hero, polished quiz flow with dramatic card hovers and bold results page.
</objective>

<execution_context>
@C:/Users/somet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/somet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-brutalist-design-polish-performance/10-RESEARCH.md
@.planning/phases/10-brutalist-design-polish-performance/10-01-SUMMARY.md
@.planning/phases/10-brutalist-design-polish-performance/10-02-SUMMARY.md
@src/app/[locale]/(public)/cities/[city]/page.tsx
@src/components/quiz/QuizResultsPage.tsx
@src/components/quiz/QuizCard.tsx
@src/components/quiz/QuizEngine.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply brutalist polish to city landing pages</name>
  <files>
    src/app/[locale]/(public)/cities/[city]/page.tsx
    src/components/city/CityHero.tsx
    src/components/city/CityFAQ.tsx
  </files>
  <action>
Use the `/frontend-design` skill. Reference Stitch city page screen from Plan 10-01.

**A) Create `src/components/city/CityHero.tsx`** — "use client":
A bold hero section for city pages:
- Full-width section with large city name in text-5xl sm:text-6xl font-black
- Subtitle about acroyoga in the city from i18n
- Thick pink accent bar under the heading (w-20 h-1 bg-brand)
- Background: subtle gradient or decorative shapes (not parallax — keep city pages simpler than homepage)
- CTA: "Find Partners in [City]" button with `btn-press` class, links to /members with city filter
- Wrap in ScrollReveal for entrance animation

**B) Update `src/app/[locale]/(public)/cities/[city]/page.tsx`:**
Read the existing 140-line page first. Enhance without removing existing content (jam listings, FAQ, structured data):
- Import and use CityHero as the top section
- Wrap existing content sections in ScrollReveal for reveal-on-scroll
- Add `card-hover` class to any card-like elements (jam previews, info blocks)
- Ensure the CityFAQ section (details/summary accordion) gets brutalist styling: bold borders, pink accent markers, text-brand on open summary
- Bold section headings: text-3xl font-black for section titles
- Maintain all existing SEO elements (JSON-LD, metadata, hreflang) — do NOT remove or modify structured data

**C) Update `src/components/city/CityFAQ.tsx`** (if it exists as a separate component, otherwise style inline in page.tsx):
- Style the details/summary accordion with brutalist aesthetics: thick left border (border-s-4 border-brand) on each FAQ item
- Summary text in font-bold, text-lg
- Open state: summary text becomes text-brand
- Smooth open/close transition if possible with pure CSS

**IMPORTANT:** All SEO elements (JSON-LD FAQPage schema, metadata, hreflang) must remain unchanged. Only visual styling changes.
  </action>
  <verify>
Run `npx tsc --noEmit` — compiles. Verify CityHero.tsx exists and imports ScrollReveal. Verify city page.tsx still contains JSON-LD schema output and all metadata. Verify card-hover class is applied to appropriate elements.
  </verify>
  <done>City landing pages have bold brutalist hero, scroll-reveal sections, card-hover effects, and styled FAQ accordion. All SEO/structured data preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Apply brutalist polish to quiz flow and results page</name>
  <files>
    src/components/quiz/QuizCard.tsx
    src/components/quiz/QuizResultsPage.tsx
    src/components/quiz/QuizProgressBar.tsx
    src/app/[locale]/(app)/quiz/challenge/page.tsx
  </files>
  <action>
Use the `/frontend-design` skill. Reference Stitch quiz screen from Plan 10-01.

**A) Update `src/components/quiz/QuizCard.tsx`:**
Read existing file first. Add brutalist hover effects:
- Apply `card-hover` class for dramatic scale + pink glow on hover
- Add `btn-press` class (or active:scale-95) for press feedback on selection
- Thick border (border-2) that becomes border-brand on hover and when selected
- Ensure the selected state is visually distinct: bg-brand/10 border-brand
- Keep all existing functionality (onClick, selected state, bilingual label)

**B) Update `src/components/quiz/QuizResultsPage.tsx`:**
Read existing file first (it's a large 8-section conversion page). Enhance with brutalist polish:
- Wrap each section (archetype, radar chart, strengths, fears, testimonials, pricing, FAQ, share) in ScrollReveal with staggered delays
- Bold section headings: text-3xl sm:text-4xl font-black
- Pink accent bars (w-16 h-1 bg-brand) under section headings
- Pricing cards: add `card-hover` class for dramatic hover
- CTA buttons: wrap in MagneticWrapper on desktop, add `btn-press` class
- Testimonial cards: add subtle `card-hover` effect
- Do NOT change any analytics tracking, share functionality, or pricing logic

**C) Update `src/components/quiz/QuizProgressBar.tsx`:**
Read existing file. Ensure the progress bar uses bg-brand for the fill color and has a smooth Framer Motion width transition (if not already animated).

**D) Verify `src/app/[locale]/(app)/quiz/challenge/page.tsx`:**
Read the page. If the quiz container needs brutalist framing (similar to auth pages — thick border, dark card), add it. If it already delegates to QuizEngine which handles layout, the changes in QuizCard and QuizProgressBar may be sufficient.

**IMPORTANT:** Do not change any quiz logic, branching, analytics tracking, or API calls. Only visual styling changes.
  </action>
  <verify>
Run `npx tsc --noEmit` — compiles. Verify QuizCard.tsx has card-hover and btn-press classes. Verify QuizResultsPage.tsx imports ScrollReveal and MagneticWrapper. Verify all quiz functionality unchanged (branching, tracking, share URL).
  </verify>
  <done>Quiz flow has dramatic card hovers and press feedback. Results page has scroll-reveal sections, bold headings, magnetic CTAs, and card-hover pricing. All quiz logic and analytics preserved.</done>
</task>

</tasks>

<verification>
- City pages have CityHero with bold typography and scroll-reveal sections
- City pages retain all JSON-LD, metadata, hreflang SEO elements
- QuizCard has dramatic hover and press feedback
- QuizResultsPage has scroll-reveal sections and magnetic CTA buttons
- `npx tsc --noEmit` passes
- No functional regressions in quiz flow or city page content
</verification>

<success_criteria>
- City landing pages match homepage brutalist aesthetic with bold hero, scroll reveals, card hovers
- Quiz cards have dramatic hover effects that feel alive
- Quiz results page has section-by-section reveal and bold brutalist typography
- All existing functionality (SEO, quiz logic, analytics) preserved
</success_criteria>

<output>
After completion, create `.planning/phases/10-brutalist-design-polish-performance/10-04-SUMMARY.md`
</output>
