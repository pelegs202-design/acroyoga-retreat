---
phase: 10-brutalist-design-polish-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/effects/MagneticWrapper.tsx
  - src/components/effects/ScrollReveal.tsx
  - src/components/effects/ParallaxLayer.tsx
  - src/components/effects/HorizontalScrollSection.tsx
  - src/app/globals.css
  - src/app/[locale]/layout.tsx
autonomous: true
requirements: [DSGN-01, DSGN-04, DSGN-06]

must_haves:
  truths:
    - "Desktop users see a thin dark scrollbar with pink thumb"
    - "Form inputs glow pink on focus across all pages"
    - "Buttons scale down on press and bounce back"
    - "Cards scale up with pink glow border on hover"
    - "All animations respect OS prefers-reduced-motion setting"
    - "Google Stitch screens are generated and saved for homepage, city, quiz, auth, header, footer"
  artifacts:
    - path: "src/components/effects/MagneticWrapper.tsx"
      provides: "Desktop-only magnetic cursor pull wrapper"
      exports: ["MagneticWrapper"]
    - path: "src/components/effects/ScrollReveal.tsx"
      provides: "whileInView fade-up reveal container"
      exports: ["ScrollReveal"]
    - path: "src/components/effects/ParallaxLayer.tsx"
      provides: "useScroll + useTransform parallax background"
      exports: ["ParallaxLayer"]
    - path: "src/components/effects/HorizontalScrollSection.tsx"
      provides: "Sticky horizontal scroll section (desktop only)"
      exports: ["HorizontalScrollSection"]
    - path: "src/app/globals.css"
      provides: "Custom scrollbar, card-hover, button-press, form-focus global styles"
      contains: "scrollbar-color"
  key_links:
    - from: "src/app/[locale]/layout.tsx"
      to: "framer-motion MotionConfig"
      via: "MotionConfig reducedMotion='user' wrapping children"
      pattern: "MotionConfig.*reducedMotion"
---

<objective>
Generate Google Stitch design prototypes for all target pages AND build the reusable brutalist effects library + global CSS foundation that every subsequent plan depends on.

Purpose: Establishes the design reference (Stitch screens) and the shared effect components (magnetic buttons, scroll reveals, parallax, horizontal scroll, card hovers, button press, form focus, custom scrollbar) so Plans 02-04 can compose pages from these building blocks without re-implementing interaction patterns.

Output: Stitch screen HTML files saved to `.planning/phases/10-brutalist-design-polish-performance/stitch-screens/`, 4 effect components in `src/components/effects/`, updated `globals.css` with brutalist base styles, and `MotionConfig` at layout level.
</objective>

<execution_context>
@C:/Users/somet/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/somet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-brutalist-design-polish-performance/10-RESEARCH.md
@src/app/globals.css
@src/app/[locale]/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate Stitch design prototypes for all target pages</name>
  <files>
    .planning/phases/10-brutalist-design-polish-performance/stitch-screens/homepage.html
    .planning/phases/10-brutalist-design-polish-performance/stitch-screens/city-page.html
    .planning/phases/10-brutalist-design-polish-performance/stitch-screens/quiz-challenge.html
    .planning/phases/10-brutalist-design-polish-performance/stitch-screens/auth-signin.html
    .planning/phases/10-brutalist-design-polish-performance/stitch-screens/header-footer.html
  </files>
  <action>
Use the Google Stitch MCP tools (`build_site` or `get_screen_code`) with project `glassy-chalice-427116-e6` to generate design prototypes for each target page. Use the `/frontend-design` skill to craft distinctive brutalist designs.

For each screen, provide Stitch a detailed prompt describing:
- Dark background (#0a0a0a), hot pink accent (#F472B6), Heebo 900 font
- Brutalist aesthetic: bold typography, raw edges, thick borders, asymmetric layouts
- The specific page content (homepage hero + sections, city landing page, quiz flow, auth forms, header/footer)

Generate at minimum:
1. **Homepage** — full-height hero with bold headline, 4-5 feature showcase sections, CTA blocks
2. **City landing page** — Tel Aviv/Kfar Saba template with local content sections, FAQ area
3. **Quiz challenge** — single question view with progress bar, option cards
4. **Auth sign-in** — centered form with brutalist framing
5. **Header + Footer** — bold nav bar and footer with brand elements

Save each generated HTML to `.planning/phases/10-brutalist-design-polish-performance/stitch-screens/` directory. These become the pixel-perfect reference for Plans 02-04.

**IMPORTANT:** Stitch generates LTR HTML. Document in each saved file a comment noting that executors must adapt for RTL (flex directions, text alignment, logical properties).

If Stitch MCP is unavailable or errors, use the `/frontend-design` skill to create detailed design specifications (color values, spacing, typography, layout) as markdown reference files instead. Do NOT block on Stitch availability.
  </action>
  <verify>
At least 5 HTML or markdown design reference files exist in `.planning/phases/10-brutalist-design-polish-performance/stitch-screens/`. Each contains specific color values (#0a0a0a, #F472B6), typography specs (Heebo), and layout details.
  </verify>
  <done>Design references exist for homepage, city page, quiz, auth, and header/footer. Each reference includes exact colors, spacing, typography, and layout specifications that executors can match.</done>
</task>

<task type="auto">
  <name>Task 2: Build reusable effects library and brutalist global CSS foundation</name>
  <files>
    src/components/effects/MagneticWrapper.tsx
    src/components/effects/ScrollReveal.tsx
    src/components/effects/ParallaxLayer.tsx
    src/components/effects/HorizontalScrollSection.tsx
    src/app/globals.css
    src/app/[locale]/layout.tsx
  </files>
  <action>
**A) Create `src/components/effects/MagneticWrapper.tsx`:**
"use client" component wrapping children with magnetic cursor pull effect. Desktop-only (uses onMouseMove/onMouseLeave). Props: `children`, `strength` (default 0.3). Uses Framer Motion `motion.div` with spring transition (stiffness: 150, damping: 15, mass: 0.1). Follow the pattern from 10-RESEARCH.md Pattern 4.

**B) Create `src/components/effects/ScrollReveal.tsx`:**
"use client" component using Framer Motion `whileInView`. Props: `children`, `className?`, `delay?` (default 0). Initial: opacity 0, y 40. Animate: opacity 1, y 0. Viewport: `{ once: true, margin: "-80px" }`. Transition: duration 0.6, ease "easeOut", delay from prop.

**C) Create `src/components/effects/ParallaxLayer.tsx`:**
"use client" component using `useScroll` + `useTransform`. Props: `children`, `speed` (default 0.4), `className?`. Wraps a container with a `ref`, `scrollYProgress` targets the ref. Background translates at `speed` ratio. Desktop only — render children directly without parallax on mobile (use a `useMediaQuery` or `hidden md:block` approach). Follow 10-RESEARCH.md Pattern 3.

**D) Create `src/components/effects/HorizontalScrollSection.tsx`:**
"use client" component for sticky horizontal scroll. Props: `children` (the items to scroll), `className?`. Uses tall outer container (h-[400vh]), sticky inner (h-screen), `useScroll` + `useTransform` to map scrollYProgress to x translation. Hidden on mobile (`hidden md:block`). On mobile, render children as a vertical stack instead. Follow 10-RESEARCH.md horizontal scroll pattern.

**E) Update `src/app/globals.css`** — ADD (do not remove existing content):
1. Custom pink scrollbar: `::-webkit-scrollbar` (width 6px, track #1a1a1a, thumb #F472B6, hover #DB2777) + Firefox `scrollbar-color: #F472B6 #1a1a1a; scrollbar-width: thin;` on `html`. Hide on mobile via `@media (max-width: 768px)`.
2. `.card-hover` class: `transition: transform 200ms ease, box-shadow 200ms ease;` + hover: `transform: scale(1.03); box-shadow: 0 0 0 1px #F472B6, 0 20px 40px rgba(244, 114, 182, 0.2);`
3. `.btn-press` class: `transition: transform 100ms ease-in;` + active: `transform: scale(0.95);` + hover: `transform: scale(1.05);`
4. Form focus glow: `input:focus, textarea:focus, select:focus { outline: none; box-shadow: 0 0 0 2px #F472B6, 0 0 12px rgba(244, 114, 182, 0.3); transition: box-shadow 200ms ease; }`
5. Add `* { scrollbar-width: thin; scrollbar-color: #F472B6 #1a1a1a; }` for Firefox.

**F) Update `src/app/[locale]/layout.tsx`:**
Import `MotionConfig` from "framer-motion". Wrap the `<NextIntlClientProvider>` block (or just children inside it) with `<MotionConfig reducedMotion="user">`. This ensures ALL Framer Motion animations across the entire app respect OS prefers-reduced-motion. This is a client component concern — since layout.tsx is a server component, create a small client wrapper component `src/components/layout/MotionProvider.tsx` with "use client" that wraps children in `<MotionConfig reducedMotion="user">`, then use it in layout.tsx.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm TypeScript compiles without errors. Verify each effect component file exists with proper "use client" directive and framer-motion imports. Verify globals.css contains scrollbar, card-hover, btn-press, and form focus styles.
  </verify>
  <done>
Four reusable effect components exist and compile. Global CSS has custom scrollbar (pink/dark), card hover glow, button press animation, and form focus glow. MotionConfig with reducedMotion="user" wraps the app. All subsequent plans can import and compose these building blocks.
  </done>
</task>

</tasks>

<verification>
- `ls src/components/effects/` shows MagneticWrapper.tsx, ScrollReveal.tsx, ParallaxLayer.tsx, HorizontalScrollSection.tsx
- `npx tsc --noEmit` passes
- globals.css contains `::-webkit-scrollbar`, `.card-hover`, `.btn-press`, `input:focus` styles
- layout.tsx (or MotionProvider.tsx) contains `MotionConfig reducedMotion="user"`
- Stitch screens directory contains design references
</verification>

<success_criteria>
- All 4 effect components export their named component and compile without errors
- globals.css has 5 new style blocks (scrollbar webkit, scrollbar firefox, card-hover, btn-press, form-focus)
- MotionConfig wraps the app with reducedMotion="user"
- Design references generated for all 5 target page groups
</success_criteria>

<output>
After completion, create `.planning/phases/10-brutalist-design-polish-performance/10-01-SUMMARY.md`
</output>
